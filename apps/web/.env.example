# URL API бэкенда. Переменная необязательна: без .env используется фолбэк '/api'
# (см. src/constants/api.ts) — сборка и демо работают полностью офлайн.
# Порт/URL бэка не согласован с командой (вопрос 12 в docs/frontend-plan.md).
VITE_API_BASE_URL=/api
