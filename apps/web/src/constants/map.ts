// Геометрия геосхемы в единицах viewBox. Карта НЕ претендует на координаты:
// форма русла условна, вертикаль передаёт только подтверждённый порядок
// течения (ТЗ §17; решение сессии 1 — линейная схема, пока координат нет).
export const MAP_VIEWBOX_WIDTH = 100
export const MAP_VIEWBOX_HEIGHT = 220

/** Поле сверху и снизу: участку, открытому вверх, нужно куда рисоваться. */
export const MAP_PADDING_Y = 30

/** Горизонтальное положение русла в viewBox. */
export const MAP_RIVER_X = 42

/** Размах меандра русла в единицах viewBox. */
export const MAP_RIVER_AMPLITUDE = 7

/** Полуширина ленты участка в единицах viewBox. */
export const MAP_CORRIDOR_HALF_WIDTH = 4

export const MAP_CAPTION =
  "Географическая схема. Порядок створов подтверждён документами; точные координаты не переданы и на схеме не утверждаются."

export const MAP_CORRIDOR_OPEN_UPSTREAM_LABEL =
  "участок открыт вверх по течению"

export const MAP_UNPLACED_TITLE = "Положение не подтверждено"

export const MAP_UNPLACED_NOTE =
  "Для этих створов и объектов место в цепочке течения не подтверждено документами, поэтому на схеме они не размещены."

/** ТЗ §4: имя объекта допустимо только с этой подписью и документами-основаниями. */
export const MAP_CANDIDATE_LABEL = "объект для проверки"

/**
 * Основание, по которому объект размещается между створами. Координат у
 * объектов нет (`location: null`), но подписи створов бюллетеня называют сброс
 * по имени — и это проверенные данные со страницы 22. Положение выводится
 * из них, и это печатается пользователю в тултипе маркера, а не умалчивается.
 *
 * Регистр удаляется, когда бэк отдаст либо координаты объекта, либо
 * `candidateObjects[].stationId` в контракте: сейчас эта привязка живёт только
 * во входе расчётного ядра и в `IncidentDetail` не попадает.
 */
export const MAP_OBJECT_PLACEMENT: Record<
  string,
  { betweenStationIds: readonly [string, string]; basis: string }
> = {
  "obj-atyrau-su-arnasy": {
    betweenStationIds: ["st-asa-0-5km-above", "st-asa-0-5km-below"],
    basis:
      "Положение выведено из подписей створов бюллетеня Казгидромета («0,5 км выше сброса» и «0,5 км ниже сброса», стр. 22), а не из координат: координаты объекта не переданы.",
  },
}
