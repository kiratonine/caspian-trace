# Integration 01 — Platform + Investigation

## 1. Result

- Platform + investigation runtime — **COMPLETE**
- Shared DB migration — **PENDING REVIEW**
- LLM provider — **DISABLED**
- Article enrichment — **NOT IMPLEMENTED**

Parts 01–08 remain wired in the same NestJS application. Deterministic investigation, evidence, replay, and JSON/HTML dossier features are integrated without enabling an external LLM transport.

## 2. Source refs

- `PLATFORM_SHA`: `8fba720ed1260d1574c0a9d5ab3e3dc7f1fce81e`
- `BACKEND2_SHA`: `62c3405b15a244808fa30b7704d9a8889e9bacac`
- Working branch: `integration/mvp`
- Node: `v20.20.2`; npm: `10.8.2`
- Preflight: `feat/backend-platform` was clean, its HEAD equalled `origin/feat/backend-platform`, and Part 08 was already committed and present on origin.
- Branch gate: `integration/mvp` existed neither locally nor on origin before `git switch -c integration/mvp`.

## 3. Git method

No pull, merge, rebase, or cherry-pick was used. Backend 2 content was read from the pinned `BACKEND2_SHA` and selectively restored only for:

- `packages/investigation-core/**`
- `apps/api/src/investigations/**`, `apps/api/src/replays/**`, `apps/api/src/export/**`
- `apps/api/test/investigations/**`, `apps/api/test/replays/**`, `apps/api/test/export/**`, `apps/api/test/investigation-features.e2e-spec.ts`
- `data/fixtures/investigation/**`
- `scripts/generate-investigation-samples.mjs`
- `docs/backend-investigation/part-02-investigation-report.md`, `docs/backend-investigation/prisma-schema-request.md`
- safe LLM subset: provider interface, schemas, `LlmService`, `DisabledLlmProvider`, and its safe tests

Explicitly skipped: Backend 2 `AppModule`, environment, health, Prisma service/schema/migrations, contracts, manifests/lock, `data/verified/**`, frontend, Gemini provider/schemas/prompts/free-tier transport, and `verify-gemini-free-tier`.

## 4. Changed files

Platform-owned integration changes:

- `apps/api/.env.example` — fixes runtime LLM mode to `disabled`.
- `apps/api/package.json` — adds the investigation-core workspace dependency and scoped integration scripts while preserving existing scripts/dependencies.
- `apps/api/prisma/schema.prisma` — additive investigation persistence compatibility.
- `apps/api/prisma/migrations/20260806010000_integrate_investigation_persistence/migration.sql` — the sole new additive migration.
- `apps/api/src/app.module.ts` — wires `LlmModule`, `InvestigationsModule`, `ReplaysModule`, and `ExportModule` alongside Parts 01–08.
- `apps/api/src/config/environment.ts` — fail-closed `LLM_PROVIDER=disabled` validation.
- `apps/api/src/incidents/incidents.mapper.ts`, `apps/api/src/incidents/incidents.types.ts` — expose additive evidence `code`/`sortOrder` through the existing incidents boundary.
- `apps/api/src/ingestion/ingestion.module.ts` — exports the existing platform ingestion-token guard.
- `apps/api/test/config.spec.ts`, `apps/api/test/incidents/incidents-row.fixture.ts`, `apps/api/test/safe-fetch/test-environment.ts`, `apps/api/test/set-env.ts` — integration-compatible environment and mapper regressions.
- `package.json`, `package-lock.json` — workspace scripts/dependency graph; lock regenerated by npm.
- `packages/contracts/src/schemas.ts`, `packages/contracts/test/contracts.spec.ts` — additive evidence contract and regression tests.
- `packages/contracts/fixtures/dossier-september.json`, `packages/contracts/fixtures/evidence-september.json`, `packages/contracts/fixtures/incident-september.json` — deterministic evidence codes/order.
- `scripts/create-backend-platform-archive.mjs`, `scripts/create-backend-platform-archive.test.mjs` — clean integration archive allowlist/exclusions/assertions.
- `scripts/verify-api-clean-start.mjs` — disabled-only LLM startup environment.
- `scripts/verify-prisma-clean-db.mjs` — disposable Integration 01 migration/DB-e2e mode.
- `docs/backend-platform/integration-01-platform-investigation-report.md` — this report.

Imported/adapted feature implementation:

- Export: `apps/api/src/export/dossier.mapper.ts`, `export.controller.ts`, `export.module.ts`, `export.service.ts`, `html.renderer.ts`.
- Investigations: `apps/api/src/investigations/admin-investigations.controller.ts`, `evidence.mapper.ts`, `file-investigation.repository.ts`, `investigation-input.schema.ts`, `investigation.ports.ts`, `investigations.controller.ts`, `investigations.module.ts`, `investigations.service.ts`, `prisma-investigation.repository.ts`.
- Replay: `apps/api/src/replays/replay.mapper.ts`, `replays.controller.ts`, `replays.module.ts`, `replays.repository.ts`, `replays.service.ts`.
- Safe LLM: `apps/api/src/llm/disabled-llm.provider.ts`, `llm-provider.ts`, `llm.module.ts`, `llm.service.ts`, `schemas/llm.schemas.ts`.
- Feature tests: `apps/api/test/export/export.spec.ts`, `apps/api/test/investigations/investigations.spec.ts`, `apps/api/test/replays/replays.spec.ts`, `apps/api/test/llm/llm.spec.ts`, `apps/api/test/investigation-features.e2e-spec.ts`.
- DB integration: `apps/api/test/integration/investigation-integration-db.e2e-spec.ts`.
- Investigation fixtures: `data/fixtures/investigation/aktau-golden.json`, `aktau-input.json`, `may-golden.json`, `may-input.json`, `september-golden.json`, `september-input.json`, `api/dossier-september.html`, `api/dossier-september.json`, `api/evidence-september.json`, `api/replay-september.json`.
- Core package: `packages/investigation-core/package.json`, `eslint.config.mjs`, `tsconfig.json`, `tsconfig.test.json`, `test/investigation-core.spec.ts`, and `src/comparison.ts`, `conclusion.ts`, `corridor.ts`, `decimal.ts`, `evidence-level.ts`, `exclusions.ts`, `hash.ts`, `index.ts`, `intervals.ts`, `normalize.ts`, `run-investigation.ts`, `station-graph.ts`, `types.ts`, `unknowns.ts`.
- Fixture generator: `scripts/generate-investigation-samples.mjs`.

The duplicate imported `apps/api/src/investigations/ingestion-token.guard.ts` was removed; only the platform guard remains.

## 5. Ownership boundaries

Backend 1 manually reconciled AppModule, environment, contracts, incidents mapper, manifests/lock, Prisma schema/migration, shared guard, test infrastructure, report, and archive. Backend 2-owned feature/core files were selectively imported and adapted only at platform boundaries. No `apps/web/**`, `data/verified/**`, applied migration, or Backend 2 platform replacement was changed.

## 6. Prisma compatibility matrix

| Backend 2 expectation | Platform state before Integration 01 | Final schema / SQL | Verification |
| --- | --- | --- | --- |
| `StationRelation.metadata` | absent; normalized relation evidence already authoritative | non-null JSONB `{}` added | clean migration and DB repository test PASS |
| `Incident.metadata` | absent | non-null JSONB `{}` added; repository preserves unknown metadata | DB idempotency/metadata test PASS |
| unique investigation version | non-unique incident indexes | unique `(incident_id,input_hash,ruleset_version)` | preflight collisions `[]`; repeated save/new-ruleset DB test PASS |
| repeated evidence codes | unique `(investigation_id,code)` | old unique dropped; ordinary index created | repeated-code DB test PASS |
| unique evidence order | no uniqueness | unique `(investigation_id,sort_order)` | preflight collisions `[]`; P2002 negative DB test PASS |
| candidate disposition evidence | no join | `investigation_candidate_object_evidence`, composite PK/FKs, RLS | persisted join DB test PASS |

The repository reads normalized `StationRelationEvidence` provenance deterministically, does not overwrite an existing relation's primary provenance, and preserves external Incident/Measurement metadata during versioned writes.

## 7. Migration safety

- Existing five platform migrations are byte-for-byte untouched.
- New folder: `20260806010000_integrate_investigation_persistence`.
- Read-only shared collision checks before migration: version collisions `[]`; evidence sort-order collisions `[]`.
- Disposable PostgreSQL 16: all six migrations applied from empty DB — **PASS**.
- Second `prisma migrate deploy`: no pending migration — **PASS**.
- `prisma migrate status`: up to date on disposable DB — **PASS**.
- Schema drift check: `No difference detected` — **PASS**.
- Shared Supabase deploy: **NOT RUN** by policy. Its status correctly reports the new migration as pending review.
- `db push`, `migrate reset`, and production-row repair were not used.

## 8. Contracts

`EvidenceStatementSchema` was extended additively with required stable `code` and non-negative integer `sortOrder`; `measurementIds` remains optional by evidence semantics and source provenance requirements remain intact. September evidence/incident/dossier fixtures and the current incidents mapper now carry these fields. Contract runtime validation passed all 26 tests.

## 9. Module wiring

The final `AppModule` retains Config, Prisma, Health, Incidents, Sources, SafeFetch, Ingestion, and Live, and adds LLM/Investigations/Replays/Export. `InvestigationsModule` imports `IngestionModule` and reuses its exported constant-time guard with `INGESTION_UNAUTHORIZED`. Static inspection found one guard and no second Prisma client.

## 10. LLM state

- `DisabledLlmProvider` only.
- No Gemini files imported.
- No external LLM call or transport.
- `LLM_PROVIDER` rejects values other than `disabled`.
- No ingestion enrichment and no article → LLM connection.

## 11. Endpoint verification

HTTP e2e (5/5) verified:

- evidence graph returns contract-valid `code`/`sortOrder` and the frozen September sample;
- recompute without/malformed token returns normalized `401 INGESTION_UNAUTHORIZED`; valid repeated recompute returns identical input hash/ruleset result;
- replay is deterministic and contract-valid;
- JSON dossier is contract-valid;
- HTML dossier is an attachment with `Content-Security-Policy: default-src 'none'` and `X-Content-Type-Options: nosniff`, with no script element;
- invalid PDF export returns normalized `400 EXPORT_FORMAT_INVALID` with request id;
- health/live, incidents, live status, and Swagger remain wired.

The feature verification uses real Nest HTTP transport with controlled repositories. Compiled production/development startup was separately exercised against disposable PostgreSQL. A manual call against shared Supabase was intentionally not attempted because the reviewed migration has not been deployed there.

Manual HTTP verification against the compiled API modules on `127.0.0.1:3099` also passed: first evidence code `NO_LOCAL_INCREASE_IN_PAIR`; unauthenticated recompute `401/INGESTION_UNAUTHORIZED`; repeated authorized recompute `idempotent=true`; replay `6` steps; JSON evidence level `L2`; HTML CSP/nosniff present; invalid export `400/EXPORT_FORMAT_INVALID`. This controlled run used the frozen repository fixture adapter and made no shared-DB write.

## 12. Tests

| Command | Result |
| --- | --- |
| `npm install --include=optional` | PASS |
| `npm ci --include=optional` | PASS; npm reported 6 pre-existing audit findings and an engine warning for transitive `@prisma/streams-local` |
| Prisma format / validate / generate | PASS |
| contracts typecheck / test / build | PASS; 26 tests |
| contracts lint | NOT RUN; package has no lint script |
| investigation-core typecheck / lint / test / build | PASS; 32 tests |
| `npm run test:investigation` | PASS; 18 tests |
| `npm run test:e2e:investigation` | PASS; 5 tests |
| `npm run test:investigation:clean` | PASS; 6 DB tests, deploy twice, no drift |
| API typecheck / lint / unit | PASS; 37 suites, 380 tests |
| API HTTP e2e | PASS; 5 suites, 57 tests |
| API unavailable-DB process e2e | PASS; 1 test |
| API build / clean production+development start | PASS |
| Manual compiled-module HTTP evidence/recompute/replay/export check | PASS |
| `npm run test:prisma:clean` | PASS; 10 base DB tests plus clean start |
| `npm run test:supabase:readiness` | PASS; 2 read-only tests |
| `npm run test:database-guard` | PASS; 3 tests |
| archive allowlist/security tests | PASS; 6 tests |
| clean archive creation and embedded report inspection | PASS; 274 files |
| `git diff --check` | PASS |

## 13. Regressions

- Verified data structure: **PASS**; 2 documents, 9 measurements, 4 relation evidence items.
- Strict seed validate-only: **PASS**, zero database writes.
- Human review gate: **BLOCKED as expected**, `0/2`; no bypass.
- Clean seed/incidents/sources DB suites: **PASS** (11/8/7 tests).
- Real private Supabase Storage smoke: **PASS**; immutable upload, duplicate verification, signed read, SHA, exact cleanup.
- SafeFetch tests/smoke: **PASS**; 7 suites/95 tests and live Kazhydromet 200 (108,463 bytes).
- Kazhydromet unit/HTTP/security and clean DB: **PASS** (6 suites/45 tests; DB 7 tests); live smoke **PASS** (32 pages, 7 candidate-only matches, human `0/2`).
- GDELT/direct tests and clean DB: **PASS** (8 suites/45 tests; DB 9 tests); live smoke **PASS** through direct fallback with 1 immutable raw article snapshot while GDELT health remained explicitly `failed`.
- Full workspace typecheck/lint/test/build: **PASS**. Workspace unit total includes API 380, contracts 26, core 32; web typecheck/lint/build passed with no frontend changes.

## 14. Security

- Recompute uses the single platform constant-time token guard and normalized unauthorized response.
- Disposable DB tests require explicit flags, reject Supabase-like hosts before connection, and clean only `test-integration01-*` records.
- Static checks found no external LLM transport, forbidden DB client, duplicate guard, or ingestion writes to IncidentSignal/Measurement/Investigation.
- HTML export security headers and attachment semantics passed.
- No service key, token, connection string, signed URL, raw upstream error, or raw article/PDF dump is included in output/archive.

## 15. Known limitations

- The integration migration is pending review and deliberately not deployed to shared Supabase.
- LLM is disabled and article signal automation is not implemented.
- Human review remains `0/2 BLOCKED`; verified seed performs no writes.
- The direct-source smoke succeeded while upstream GDELT remained explicitly failed; the fallback did not rewrite GDELT health.

## 16. Archive

- Archive: `artifacts/caspian-trace-backend-platform-clean-2026-08-06T10-10-28-071Z.tar.gz`
- Allowlist entries: 274.
- Includes integration modules/core/tests/fixtures, migration, report, and Integration 01 TODO.
- Excludes frontend, secrets/env, node_modules/dist/coverage, generated Prisma client, raw HTML/PDF/GDELT dumps, artifacts, Gemini provider, and Gemini verification transport.

## 17. Git status

The initial Integration 01 implementation was committed and pushed to
`integration/mvp` as:

`ba739cc feat(api): integrate investigation runtime`

The GDELT/direct public article acceptance hardening was committed and
pushed to `integration/mvp` as:

`6b71b80 fix(api): harden public article acceptance`

No merge, rebase, force push, branch deletion, or shared database reset
was performed.

## 18. Next handoff

Integration 02:

```text
optional ArticleSignalEnricher port
→ LlmService candidate
→ no persistence initially
```

## 19. Critical post-review fixes

- production filesystem fixture fallback removed;
- current `Investigation.id` supported by evidence, recompute, replay and export;
- historical investigation versions fail closed;
- exact incident scope metadata is mandatory;
- every scoped station, relation, measurement and candidate ID must exist;
- replay cache is normalized by stable incident ID;
- equivalent ISO timestamps are normalized to UTC before hashing;
- missing and empty incident unknown collections are canonicalized identically;
- investigation input datetimes are validated as offset-aware ISO timestamps;
- ruleset version was increased from `1.2.0` to `1.2.1`;
- golden and API fixtures were regenerated;
- recompute remains idempotent after a PostgreSQL roundtrip.

## 20. GDELT/direct acceptance hardening

Accepted public articles now require all of the following:

- parser completed successfully;
- requested region matched explicitly;
- pollution relevance marker matched;
- direct-source publication time matched the requested window.

Parser-failed and irrelevant articles retain their immutable source
snapshots and provenance but are excluded from accepted API documents.

Added bounded run metadata:

- `irrelevantCount`;
- `parserFailureCount`.

A parser failure produces:

- `acceptedCount = 0`;
- no entry in response `documents`;
- failed run when no other article is accepted;
- degraded source health;
- retained immutable raw snapshot.

Regression coverage includes:

- same-region article without a pollution marker;
- parser failure with an otherwise usable publication date;
- parser-failed GDELT snapshot persistence and exact cleanup;
- direct and GDELT accepted-document boundaries.
