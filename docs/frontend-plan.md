# План фронтенда «Каспийский след»

> **04.08.2026 пришло обновлённое ТЗ** от бэкендера: `docs/caspian-trace-development-roadmap.md`
> (общий план команды) и `docs/caspian-trace-role-frontend.md` (план моей роли, этапы F0–F9).
> Разбор — в `docs/context/`: приоритет документов (`sources-of-truth.md`), что закрыто
> и что изменилось (`2026-08-04-updated-spec.md`), контракт одной страницей
> (`frontend-api-contract.md`), сверка этапов (`frontend-role-checklist.md`).
> Читать эту папку в начале сессии, если задача касается контракта, досье или интеграции.

## Состояние на конец последней сессии

**Сессия 14 (05.08.2026) — слияние `feat/backend-investigation`, данные ядра. Завершена.**

Ветка Backend 2 (расчётное ядро `packages/investigation-core`, Prisma-персистенция,
эндпоинты evidence/replay/export) влита в `frontend`. Разбор конфликтов — в нашу
пользу по архитектуре, в их — по данным.

- **Вопрос 1 (порядок створов) закрыт частично.** Не полем `riverOrder`, а
  проверенными попарными связями `data/verified/atyrau-2025-*-station-relations.json`
  (sha256, страница 22/24, дословная выдержка, основание). Цепочка сентября:
  1 км выше Атырау → 0,5 км выше сброса → 0,5 км ниже сброса → 1 км ниже Атырау.
  Осетровый завод и Дамба в связи не входят — остаются `riverOrder: null`
  и показываются группой без ранжирования. У схемы появилось третье состояние
  подзаголовка: «порядок подтверждён не для всех створов».
- **Вопрос 8 закрыт: майская страница — 24.** Плюс настоящие sha256 обоих
  бюллетеней и дословные выдержки на каждое измерение, поэтому в сиде
  `verified: true`, а майские числа теперь тоже ведут на `#page=24`.
- **Сентябрь вернулся на L2** (`september-golden.json`, ruleset 1.1.0):
  коридор открыт вверх до створа «1 км выше Атырау», **два** опровержения
  и — важное — **ноль** утверждений `supports`. Ядро не выпускает «факт»
  на само измерение, поэтому блок 3 панели пуст, и его пустое состояние
  переписано: «Подтверждающих утверждений расчётное ядро не выпустило».
- **Май (L3) и Актау (L0) тоже взяты из golden целиком**: у мая пустой список
  пробелов, у Актау — три пробела кодами и более сухой вывод (формулировка
  «есть сообщения и модель волн» из ТЗ §7.6 в ядро не попала — вопрос 20).
- **Реплей: 6 шагов вместо 5**, офсеты 0/5/10/15/20/25 с — по шагу `inference`
  на каждое правило. `REPLAY_STEP_OFFSETS_MS` остался разметкой пустой шкалы
  для событий без сценария.
- **Приняты `EvidenceStatement.code` и `sortOrder`** (обязательные), добавился
  `HealthReadySchema`. Утверждения ядра приходят с `generatedBy: 'rule_engine'`.
- **Три эндпоинта Backend 2 подняты** (evidence, replay/start, export json+html),
  но `VITE_DATA_MODE=api` включать нечем: `GET /api/incidents` и `/incidents/:id`
  у Full-stack 1 всё ещё нет. Их стабильные ответы лежат
  в `data/fixtures/investigation/api/` — сид повторяет их дословно.
- **Откачено из ветки бэка:** подмешивание `EvidenceGraph` в detail (стирало
  три створа со схемы и две публикации из «Источников»), `DossierActions`
  в шапке панели, `apiDownload`/`src/api/exports.ts` (ходили в сеть мимо
  границы `client.ts`), их `vite.config.ts`-прокси (дубль нашего), возврат
  удалённых `src/types/*`. Обоснования — в `docs/decisions.md`.
- Проверено: `typecheck`, `lint`, `prettier --check`, `build` зелёные;
  Playwright 1280×720: сентябрь L2 с коридором и линией из четырёх створов
  плюс группа из трёх без ранжирования, май L3 с коридором между парой,
  Актау «недостаточно данных» с новыми текстами, реплей по шагам до L2
  и вывода, досье печатает 7 измерений со стр. 22 и SHA-256 бюллетеня,
  дев-превью схемы обновлено; в консоли только `STUB:`-варны.
- **Осталось открытым:** вопрос 10 (формула цвета), 15 (тестовая
  инфраструктура), 17 (ESM-выход пакета), 18 (две формы `unknowns`),
  19 (`react-router` audit), плюс новые 20 (вывод Актау) и 21 (эндпоинты
  ленты и detail — без них F3/F5 не начать).
- **Следующая задача (сессия 15): F6, состояния источников.** Решено 05.08.2026:
  берём её, а не F8 — тестовой инфраструктуры в `apps/web` всё ещё нет
  (вопрос 15, ставит Full-stack 1), тогда как для F6 всё готово:
  `LiveStatusSchema` заморожена, `fetchLiveStatus` с обеими ветками режима
  написан в сессии 13, `staleTime` 60 с выставлен, не хватает только UI.
  Что помнить: в фикстуре все четыре источника — `never_run` с
  `cacheAvailable: false`, поэтому на реальном экране будет ряд «ни разу
  не обновлялся», а healthy / degraded / rate_limited / failed показываем
  в дев-превью. Выдумывать «зелёные» источники в сиде ради картинки нельзя.
  Хвосты F1 (drag шкалы реплея, `prefers-reduced-motion`) — концом той же
  сессии, если останется время. F8 и F3/F5 — сессии 16+, обе ждут других.

<details>
<summary>Сессия 13 (04.08.2026) — переезд на `packages/contracts` + API-слой F2. Завершена.</summary>

Бэкендер прислал ветку `feat/backend-platform`: каркас NestJS и — главное —
**`packages/contracts`** с Zod-схемами и фикстурами всех эндпоинтов. Ветка влита
в `frontend` (конфликт modify/delete по `CLAUDE.md` разрешён в пользу нашей
версии — файл описывает зоны ответственности троих и удалён был не решением,
а состоянием чужой рабочей копии).

- **Типы больше не пишутся во фронте.** `src/types/*` (8 файлов копий ТЗ §8)
  удалены, остался баррель-реэкспорт из пакета + производный `ReplayStepType`.
  `src/api/contracts.ts` — тонкий слой реэкспорта; из своего в нём только
  `IncidentListParams` (форма запроса, её в пакете нет).
- **Закрылись вопросы 3, 4, 5, 6, 7, 11, 12, 14**: payload реплея совпал с нашим
  провизорным юнионом дословно (и офсеты `[0, 5000, 12000, 22000, 32000]` тоже),
  `IncidentSummary.period` подтверждён, `corridorBounds` подтверждён, форма
  ошибки — `{code, message, requestId}`, общий пакет = `packages/contracts`
  (владелец Full-stack 1), порт API 3000 + CORS на 5173.
- **`provenance` решён не так, как мы просили:** в `IncidentDetail` его нет
  (схема `.strict()`), `rulesetVersion`/`inputHash` едут полями `Dossier`.
  Пункт 12 досье читает их из `DossierModel.provenance` (пока `null` → «не
  передана расчётным ядром»), печатная страница по-прежнему не зависит от
  эндпоинта экспорта.
- **Состав досье-JSON переписан:** наш `DossierExport { generatedAt, disclaimer,
  incident }` отменён, теперь плоский `Dossier` пакета. Проверено скачиванием:
  14 полей, L1, 7 измерений, `unknowns` парами `{code, text}`.
- **Сид приведён к схемам** (и теперь сам проверяется ими — `parseSeed`):
  `sha256: null` вместо `''`, `fetchedAt: null`, `status: 'unverified'`
  (схема запрещает `verified` без хэша и даты скачивания); у измерений появились
  `rawValueText`, `qualityClass`, `verified: false` и `sourceExcerpt: null`
  (наша «цитата» была реконструкцией строки таблицы, а не выдержкой);
  у створов `relationType: 'neutral'` и `locationSourceDocumentId: null`.
- **Сентябрь понижен с L2 до L1** вслед за фикстурой бэка: убраны коридор,
  опровергнутая версия и пространственный вывод — они держались на порядке
  створов, который не верифицирован. Уровни на переключателе теперь L1 ↔ L3.
  Май (L3, парный интервал) и Актау (L0) не изменились. **Это самая заметная
  потеря сессии**: из демо ушли лента коридора на схеме и непустой блок 4
  «Что не подтверждается». Вернутся ответом на вопрос 1 — ветки UI живы,
  проверены на майском кейсе и включатся правкой сида.
- **API-слой F2 сделан целиком:** `VITE_DATA_MODE` (по умолчанию `seed` —
  демо обязано работать без бэкенда), проверка ответа схемой на границе,
  `AbortSignal.any` + `AbortSignal.timeout(8 с)`, нормализованная
  `ApiRequestError {code, message, requestId, status}` (отмена запроса ошибкой
  не считается — наверх уходит исходный `AbortError`), фабрика `queryKeys`,
  `staleTime` 60 с у live-status, Vite proxy `/api → localhost:3000`.
- **Найдено и починено:** пакет собирается в CommonJS, и dev-сервер не отдавал
  из него именованные экспорты («does not provide an export named
  ApiErrorSchema»). Прод-сборка справлялась сама, поэтому расхождение вылезало
  только в dev; лечится `optimizeDeps.include` в `vite.config.ts`.
- Проверено: `typecheck`, `lint`, `prettier --check`, `build` зелёные;
  Playwright 1280×720 свет/тьма: три колонки, сентябрь L1 без коридора,
  май с границами участка и без якоря страницы, Актау «недостаточно данных»,
  реплей по шагам с новыми текстами и уровнями, досье печатает все 12 разделов,
  JSON скачивается и проходит `DossierSchema`; в консоли только `STUB:`-варны.
- **Цена переезда:** бандл вырос с ~523 kB до 884 kB (gzip 234 kB) — zod и все
  схемы пакета попадают в сборку целиком, потому что пакет CommonJS и почти
  не поддаётся tree-shaking. Для офлайн-демо с локального диска приемлемо;
  просьба к Full-stack 1 добавить ESM-выход — в `docs/questions-to-fullstack.md`.
- **Блокеры на момент коммита сессии:** вопрос 1 (порядок створов), 8 (страница
  майского бюллетеня), 10 (формула цвета), плюс новые: тестовая инфраструктура
  и майская/актауская фикстуры. **Часть из них закрылась через час — см. ниже.**

### Разведка перед сессией 14 (вечер 04.08.2026)

После пуша сессии 13 подтянул remote: `feat/backend-platform` получила коммит
`75cc68d` (Prisma, миграции, `/api/health/ready`; в контракте — только
`HealthReadySchema`, нас не задевает), и **появилась ветка
`feat/backend-investigation`** (Backend 2, два коммита): расчётное ядро
`packages/investigation-core` плюс поднятые эндпоинты evidence, replay
и export json/html. Ветку НЕ сливал — разбор оставлен на сессию 14.

**Закрылось нашими вопросами:**

- **Вопрос 1 (порядок створов).** Не через `riverOrder`, а отдельными
  проверенными фикстурами `data/verified/atyrau-2025-*-station-relations.json`:
  попарные связи upstream → downstream, у каждой sha256 документа, страница,
  дословная выдержка и основание. Сентябрь: 1 км выше Атырау → 0,5 км выше
  сброса → 0,5 км ниже сброса → 1 км ниже Атырау. Осетровый завод и Дамба
  в связи не входят — для них порядок по-прежнему не подтверждён, и это
  осознанно («направления связей не выводятся из порядка строк или названий»).
- **Вопрос 8 (страница майского бюллетеня) — 24.** Плюс sha256 обоих бюллетеней
  и дословные выдержки на каждое измерение. Значит `sourcePage`, `sha256`
  и `sourceExcerpt` в сиде можно заполнить настоящими значениями, а документы
  поднять до `verified`.
- **Сентябрьский L2 возвращается.** Golden ядра
  (`data/fixtures/investigation/september-golden.json`): L2, коридор открыт
  вверх до створа «1 км выше Атырау», **два** опровержения вместо одного
  (пара у сброса без роста; максимум выше объекта), `rulesetVersion: '1.0.0'`
  и настоящий `inputHash`. Май — L3 с коридором между парой (совпал с нашим),
  Актау — L0. То есть лента коридора, непустой блок «Что не подтверждается»
  и пункт 12 досье возвращаются в демо.

**Что придётся разгребать в сессии 14:**

1. **Контракт снова сдвинулся:** `EvidenceStatement` получил обязательные
   `code` (маска `^[A-Z][A-Z0-9_]*$`) и `sortOrder`. Наш сид на этом упадёт —
   `parseSeed` поймает сразу, как и задумано.
2. **Backend 2 писал в `apps/web/**`** поверх нашего до-сессионного кода:
   `client.ts` (добавлен `apiDownload`), новый `src/api/exports.ts` (дубль
   нашего `export.ts`), `vite.config.ts` (свой прокси), `seed-data.ts`
   и `types/evidence-statement.ts` (файл удалён нами). Главное: из
   `investigations.ts` и `replays.ts` **вырезаны seed-ветки** — это ломает
   критерий приёмки «демо работает без бэкенда». Конфликты разрешать в нашу
   пользу по архитектуре (оба режима остаются) и в их — по данным.
3. Ветка собрана от `9b7d6d6`, то есть без Prisma-коммита; `schemas.ts`
   правили обе ветки бэка — разъезд между ними тоже придётся учесть.
4. Фикстуры в `packages/contracts` остались на L1, а golden ядра — L2:
   расхождение внутри их же данных, уточнить, что отдаст API.
5. Во всех verified-данных `verificationStatus:
   automated_source_checked_human_review_pending`, в манифесте
   `requiredHumanReviewers: 2, completedHumanReviewers: 0` — проверено
   автоматом, человеком ещё нет. На демо это стоит проговаривать честно.

- **План на сессию 14:** слить `feat/backend-investigation`, разобрать конфликты,
  принять `code`/`sortOrder`, майскую страницу 24, sha256 и выдержки, вернуть
  сентябрь на L2 с коридором и двумя опровержениями. F6 (состояния источников)
  и F8 (тесты) сдвигаются на сессии 15+. — **Выполнено, см. верх файла.**

</details>

<details>
<summary>Сессия 12 (04.08.2026) — печатное досье. Завершена.</summary>

- **Маршрут `/dossier/:id`** (`DOSSIER_ROUTE`, `dossierPath`, `incidentPath`
  в `constants/routing.ts`). Грузится вместе с приложением, не lazy: на демо
  страница должна открываться мгновенно, без ожидания чанка. Ссылка «Досье» —
  в шапке правой панели, возврат — на `/?incident=<id>` (выбор сохраняется).
- **`DOSSIER_SECTIONS` переписан под 12 пунктов роадмапа §21.1**; как
  и `CONCLUSION_SECTIONS`, массив и есть контракт. Пункты 1–2 (титул
  и оговорка) помечены `lead: true` — они открывают лист и не получают
  заголовка раздела, но остаются в списке, чтобы полноту состава было видно
  в одном месте.
- **`features/dossier/`**: `dossier-model.ts` (соединение измерений со
  створами и документами, хронология сигналов, объекты с документами-
  основаниями, границы участка), `DossierDocument.tsx` (разделы),
  `SignalTimeline`, `MeasurementTable`, `CandidateObjectList`,
  `DossierSources`, `DossierPage.tsx` (маршрут + печать + JSON).
- **Утверждения и список источников берутся из `buildPanelModel`** — бумага
  и экран обязаны показывать одни и те же основания.
- **Участок печатается внутри «Итоговой формулировки»**: отдельного пункта
  в §21.1 нет, но границы коридора — суть вывода; рядом оговорка, почему
  карты нет (координаты не подтверждены).
- **Пункт 12 (`rulesetVersion`/`inputHash`)**: в контракт добавлено
  провизорное `IncidentDetail.provenance` (вопрос 14), в seed — `null`,
  в досье печатается «не передана расчётным ядром». Хэш не выдумываем.
- **Шестая заглушка `src/api/export.ts`** (`fetchDossierJson`) + `lib/download.ts`:
  «Скачать JSON» отдаёт файл через blob-URL, без сети — работает офлайн.
- **Печать:** `@media print` в `index.css` — светлые переменные для обеих тем
  (иначе досье из тёмной темы печаталось бы белым по белому), `@page A4`
  с полями 16 мм; панель действий `print:hidden`; `break-inside-avoid`
  на строках таблицы, утверждениях, сигналах и источниках,
  `break-after-avoid` на заголовках разделов; URL источников печатаются
  текстом — на бумаге ссылку не нажать.
- **Починено:** бейдж уровня из тёмной темы печатался светло-янтарным
  по белой бумаге — у цветных тонов `EvidenceLevelBadge` появилась print-пара.
- **Починено:** `Button render={<Link/>}` из Base UI даёт `<a role="button">`
  (ссылка объявляется кнопкой, ломается «открыть в новой вкладке») — обе
  навигационные кнопки переведены на `buttonVariants` + `Link`.
- Проверено: `typecheck`, `lint`, `prettier --check`, `build` зелёные,
  дев-чанков в прод-бандле нет; **печать проверена настоящим PDF** (Chrome,
  A4, 3 страницы): ни одно утверждение и ни одна строка таблицы не разорваны,
  URL и SHA-строки видны целиком; свет/тьма на 1280×720 и 1440×900 без
  горизонтальной прокрутки; JSON скачивается (`dossier-inv-atyrau-2025-05.json`,
  значения 0,114/0,193, L3, `provenance: null`); Актау печатает все 12 разделов
  с осторожными пустыми состояниями; реплей и главный экран без регрессий;
  в консоли только `STUB:`-варны.
- **Следующая задача (сессия 13): остаток F2 — API-слой** (`VITE_DATA_MODE`,
  Zod на границе сети, `AbortSignal` + timeout, нормализованная ошибка
  `{code,message,requestId}`, фабрика query keys, `staleTime` 60 с
  для live-status). Это подготовка к контрольной точке C.
- **Блокеры:** вопросы 1 (порядок створов), 4 (связь периодов участка),
  8 (страница майского бюллетеня); добавился 14 (`provenance`).

</details>

<details>
<summary>Сессия 11 (04.08.2026) — контрактные правки перед досье. Завершена.</summary>


Досье писалось бы под отменённый контракт, поэтому сессия ушла на сквозные
правки форм данных (роадмап §7, §6.2–6.3).

- **`Measurement` приведён к `MeasurementSchema` роадмапа §7:** `sourcePage:
  number | null` (сентинел `0` отменён), `sampledAt: string | null` +
  отдельное `sampledPeriod: string | null`, `unit`/`matrix` — свободные строки.
  Удалены `qualityClass` и `verified`: в утверждённой схеме их нет, ни один
  компонент их не читал, а тип, обещающий `boolean` там, где API ничего не
  пришлёт, — это та же ложь, что и `as T`.
- **`IncidentSignal`:** добавлены `verificationStatus: 'conflicting'`
  и `observedPeriod` (в схеме бэка `observed_period` — та же пара «точная
  дата / период», что у измерений); `extractionMode` — `'llm_verified'`
  вместо `'llm'` по check-ограничению таблицы; `phenomenon` — строка
  (в БД это `text` без check, в отличие от соседних колонок).
- **Справочники получили фолбэк на сырую строку:** `unitLabel`,
  `matrixLabel` (+ ключ `surface_water` — значение по умолчанию в схеме БД),
  `phenomenonLabel`. Незнакомое значение показывается как есть — `undefined`
  на экране хуже сырого кода.
- **`formatSampledAt` → `formatSampledDate` / `formatObservedDate`**: берут
  объект целиком и возвращают `string | null` (точная дата днём, период
  месяцем, `null` — датировать нечем). Вызывающие места сами решают, что
  делать с `null`: в подписи схемы и тултипе даты просто нет.
- `sourceHref`/`hasConfirmedPage` принимают `number | null`; проверка `> 0`
  осталась как страховка (нумерация страниц в контракте положительная).
- **Найдено и починено: `npm run typecheck` ничего не проверял** — `tsc
  --noEmit` на solution-конфиге с `"files": []` компилирует ноль файлов.
  Скрипт переведён на `tsc -b`; реальную проверку до сих пор делал только
  `build`.
- Проверено: `typecheck` (уже настоящий), `lint`, `build` зелёные;
  Playwright 1280×720 свет/тьма: сентябрьские числа ведут на `#page=22`
  и тултип пишет «стр. 22», майские — без якоря и с «(страница уточняется)»,
  подписи периодов «сентябрь/май 2025 г.» на месте, хронология сигнала
  «наблюдалось 1 сентября 2025 г.», экран Актау и реплей (ручной переход по
  шагам, значения → вывод) без регрессий, все четыре дев-превью без ошибок;
  в консоли только `STUB:`-варны.
- **Следующая задача (сессия 12): печатное досье** — маршрут `/dossier/:id`,
  12 секций (роадмап §21.1), `@media print`, `LEGAL_DISCLAIMER`, «Скачать
  JSON» (шестая заглушка `src/api/export.ts`). В рабочей копии лежит
  незакоммиченный черновик `src/constants/dossier.ts` под старый состав из
  9 пунктов ТЗ §12 — строки корридора/хронологии/таблицы переиспользуются,
  но `DOSSIER_SECTIONS` переписывается под 12 секций.
- **Блокеры прежние:** вопросы 1, 2 (в части «настоящей связи периодов» — 4).

</details>

<details>
<summary>Сессия 10 (04.08.2026) — переключение май/сентябрь со сменой вывода. Завершена.</summary>

- **В провизорный контракт добавлено поле `IncidentSummary.period`** (ISO до
  месяца, `null` у Актау): без него список событий отличал май от сентября
  только разбором заголовка. Значения в seed реальные — те же месяцы, что
  у измерений §5. Ответ команды по вопросу 4 правит `contracts.ts` + `seed-data.ts`.
- `features/comparison/comparison-model.ts` — сопоставимые события выводятся
  из полей API (одна область + один показатель + известный период), связь
  май↔сентябрь не выдумывается; `areComparable` — та же проверка для пары.
- `PeriodSwitcher` (над схемой): периоды участка кнопками, уровень стоит
  прямо на кнопке — разница L2/L3 видна ещё до клика, и переключение не
  выглядит сменой фильтра. Пишет тот же `?incident=`, что и лента.
- `VerdictChange` + `hooks/use-verdict-change.ts` — плашка «Вывод изменился»
  при переходе между сопоставимыми периодами: было → стало с периодом,
  уровнем и дословными выводами обоих событий, плюс объяснение §14, что
  вывод следует за фактами измерений, а не переоценивает объекты.
  Живёт МЕЖДУ шапкой панели и прокручиваемой областью — не теряется, если
  панель прокручена. Закрывается крестиком, при новом переходе появляется
  снова. Предыдущий выбор берётся сравнением с прошлым рендером (штатный
  приём React), данные — из кэша TanStack Query, без единого нового запроса.
- Во время реплея плашка не показывается: там своя хронология.
- Дев-превью `/dev/comparison`: переключатель, кейс «пары нет» (Актау),
  оба направления перехода.
- Проверено: `typecheck`, `lint`, `build` зелёные, дев-чанков в прод-бандле нет;
  Playwright 1280×720 свет/тьма: переключение в обе стороны меняет все три
  колонки и показывает плашку L2↔L3, «скрыть» работает, у Актау нет ни
  переключателя, ни плашки; в консоли только `STUB:`-варны.
- **Следующая задача (сессия 11): печатное досье** — маршрут `/dossier/:id`,
  `@media print`, юр. оговорка `LEGAL_DISCLAIMER`; print preview проверить
  сразу, не в последний час (блок 36–45 ч).
- **Блокеры прежние:** вопросы 1, 2, 3; вопрос 4 частично закрыт провизорным
  `period` — команде подтвердить или заменить настоящей связью расследований.

</details>

<details>
<summary>Сессия 9 (04.08.2026) — реплей: плеер, шкала, клавиатура. Завершена.</summary>

- `src/stores/replayStore.ts` (единственная зона Zustand): сценарий, индекс шага,
  `status` (paused/playing/finished) и позиция **парой якорей** —
  `anchorPositionMs` (зафиксирована последним play/pause/seek) +
  `anchorTimeMs` (`performance.now()` момента старта, null = стоим). Текущая
  позиция всегда вычисляется `getReplayPositionMs()`, поэтому пауза и перемотка
  не копят дрейф. Действия: `start`/`play`/`pause`/`seekToStep`/`advanceToStep`/`exit`.
- `src/features/replay/useReplayPlayback.ts`: `useReplayPlayback` — один
  `setTimeout` до границы следующего шага, задержка каждый раз считается от
  абсолютного `offsetMs` (не цепочка относительных таймеров);
  `useReplayPositionMs` — плейхед через `requestAnimationFrame`, живёт вне
  стора, чтобы 60 fps не дёргали подписчиков.
- `src/features/replay/replay-frame.ts` — «кадр» реплея: что уже показано к
  текущему шагу (`visibleMeasurementIds`, `visibleSourceDocumentIds`,
  `inferenceReached`, `conclusionText`, `evidenceLevel` из payload шага).
  `useReplayFrame(incidentId)` отдаёт кадр только для СВОЕГО события,
  `projectDetailForReplay` — проекция detail на кадр.
- `ReplayTimeline` переписан в плеер: play/пауза/перезапуск, кнопка выхода,
  кликабельные маркеры (клик без запущенного реплея = ручной режим на этом
  шаге), плейхед и залитая часть шкалы, строка текущего шага с дословным
  текстом payload и компактным `EvidenceLevelBadge`; клавиатура — пробел
  (пуск/пауза), ←/→ (по шагам), обработчик игнорирует поля ввода и не
  перехватывает пробел на кнопках/ссылках.
- Панели читают кадр **селектором поверх загруженных данных, без рефетча**:
  схема показывает только «загруженные» шагами значения и коридор (он —
  результат шага inference), панель гейтит блоки 1/3/4/5 строкой
  `REPLAY_PENDING`, уровень и вывод берёт из payload шага.
- Сценарий читается через `replayScenarioQueryOptions` (POST, но сценарий
  неизменяем → query): недоступный сценарий заранее гасит кнопку play.
  Смена события гасит активный реплей (защита от рассинхрона колонок).
- **Починена регрессия вёрстки:** футер как строка грида получал минимальную
  ширину по содержимому, и длинный текст вывода растягивал весь экран вбок
  (горизонтальная прокрутка на 1280×720). Лечится `min-w-0` на `<footer>`;
  строка шага — блок с `truncate`, а не flex.
- Проверено: `typecheck`, `lint`, `build` зелёные, дев-чанков в прод-бандле нет;
  Playwright 1280×720 свет/тьма: все 5 шагов по §14 (L0 → L1 → L1+значения →
  L2+коридор+факты → вывод), пауза держит позицию и продолжает с неё же,
  пробел/стрелки работают, май и Актау без сценария дают отключённое
  управление с осторожной формулировкой, выход возвращает полные данные;
  в консоли только `STUB:`-варны.
- **Следующая задача (сессия 10): переключение май/сентябрь** — выполнена.
- **Блокеры прежние:** вопросы 1 и 2; вопрос 3 (payload реплея) теперь
  особенно важен — весь плеер построен на провизорном `TypedReplayStep`.

</details>

<details>
<summary>Сессия 8 (04.08.2026) — экран Актау «недостаточно данных». Завершена.</summary>

- `src/features/aktau/InsufficientDataScreen.tsx` — полноэкранное состояние
  центральной колонки для события без единого створа: область
  (`REGION_LABELS`), заголовок события, `EvidenceLevelBadge` с расшифровкой
  в тултипе, дословный осторожный вывод с бэка цитатой и `InsufficientData`
  с причинами (unknowns). Компонент generic: кейс Актау не хардкодится,
  любое событие без геометрии получает тот же экран.
- `RiverSchemeContent` при `stations.length === 0` рендерит экран вместо
  заголовка «Линейная схема реки» + голого бокса: заголовок про реку для
  прибрежного кейса был неправдой, а полноэкранное состояние читается как
  осознанный отказ (§13: жюри должно увидеть, что система не генерирует
  убедительный ответ без данных). Заодно исправлено центрирование при
  переполнении (min-h-full вместо items-center на overflow-контейнере).
- Отдельная галерея не понадобилась: кадр «Актау — недостаточно данных»
  в `/dev/river-scheme` показывает новый экран автоматически.
- Проверено: `typecheck`, `lint`, `build` зелёные; Playwright 1280×720
  свет/тьма: экран Актау по deep-link, сентябрьская схема без регрессий,
  кадр галереи скроллится в рамке; в консоли только `STUB:`-варны.
- **Следующая задача (сессия 9): реплей** — выполнена (описание — блоком выше).
- **Блокеры прежние:** вопросы 1 и 2; для типизации payload реплея открыт
  вопрос 3 (провизорный `TypedReplayStep` уже есть).

</details>

<details>
<summary>Сессия 7 (04.08.2026) — лента сигналов и расследований. Завершена.</summary>

- `src/features/feed/`: `SignalFeed` (контейнер: список событий + запись
  выбора; `SignalFeedContent` — презентационная часть для дев-превью;
  `IncidentCard` — карточка события: заголовок, показатель + область,
  компактный `EvidenceLevelBadge`; «растянутая кнопка» after:inset-0 —
  бейдж с тултипом и ссылки сигналов остаются настоящими интерактивами),
  `SignalCard` (публичный сигнал: явление + статус проверки из
  `VERIFICATION_STATUS_META`, дословная цитата, локация и хронология
  наблюдалось/сообщено, `SourceLink` на первоисточник — §16 п.5 и для сигналов).
- Клик по карточке пишет `?incident=` через `setSearchParams` (push —
  история выбора листается «назад»); хук `use-selected-incident-detail`
  теперь возвращает и `selectedIncidentId` — подсветка карточки и данные
  схемы/панели идут из одного источника истины; `aria-current` на выбранной.
- Сигналы разворачиваются под карточкой ВЫБРАННОГО события из его detail
  (списковый эндпоинт сигналы не отдаёт — вопрос 2; detail уже в кэше
  TanStack Query для схемы и панели). Пустое состояние — осторожная
  формулировка в `constants/feed.ts`; `REGION_LABELS` — `constants/regions.ts`.
- Дев-превью `/dev/feed`: три состояния выбора (сентябрь с сигналом,
  май и Актау без), клики работают на локальном useState, URL не трогают.
- Проверено: `typecheck`, `lint`, `build` зелёные, дев-чанков в прод-бандле
  нет; Playwright 1280×720 свет/тьма: клик по маю переключает все три колонки
  разом, deep-link `?incident=` переживает прямую загрузку, `aria-current`
  проставлен; в консоли только `STUB:`-варны.
- **Следующая задача (сессия 8): экран Актау «недостаточно данных»** —
  состояние уже работает во всех трёх колонках, довести до полноценного
  демо-экрана (§7.6/§13) и закрыть блок 24–36 ч; после него — реплей.
- **Блокеры прежние:** вопросы 1 и 2.
</details>

<details>
<summary>Сессия 6 (04.08.2026) — правая панель §13 на данных выбранного события. Завершена.</summary>

- `src/hooks/use-selected-incident-detail.ts` — общий хук выбора события
  (`?incident=` → первое в списке); `RiverScheme` переведён на него, панель
  и схема гарантированно показывают одно и то же событие (запросы
  дедуплицируются TanStack Query по ключу).
- `src/features/conclusion/`: `panel-model.ts` (утверждениям разворачиваются
  измерения-основания со створом и документом), `StatementList` (один компонент
  на блоки 3–4 — текст утверждения с бэка дословно + кликабельные значения под
  ним, §16 п.5 работает и в панели), `SourcesList` (блок 6, страница — из
  ссылающегося измерения: сентябрь «стр. 22», май скрыт сентинелом 0),
  `ConclusionPanel` (контейнер + `ConclusionPanelContent` для дев-превью;
  секции строго из `CONCLUSION_SECTIONS`).
- Блок 2 — `EvidenceLevelBadge` + видимая расшифровка уровня из
  `EVIDENCE_LEVEL_META` (§13 «с расшифровкой», не только в тултипе).
- Пустые состояния — осторожные формулировки в `constants/panel.ts`
  («Ни одна конкретная версия пока не исключена фактами» — состояние данных,
  не оценка невиновности); Актау в панели полностью рабочий: L0 + пустые
  блоки + unknowns.
- `DATA_LOAD_ERROR` перенесён в `constants/strings.ts` (общий для колонок),
  `SCHEME_LOAD_ERROR` удалён.
- Дев-превью `/dev/conclusion`: сентябрь (L2, исключённая версия),
  май (L3), Актау (L0, пустые состояния) в ширине реальной колонки.
- Проверено: `typecheck`, `lint`, `build` зелёные; Playwright 1280×720
  свет/тьма: главный экран (схема + панель на одном событии), галерея панели;
  в консоли только `STUB:`-варны.
</details>

<details>
<summary>Сессия 5 (04.08.2026) — линейная схема реки v1. Завершена.</summary>

- `src/api/queries.ts` — `queryOptions` списка событий и деталей события; все
  колонки главного экрана читают одно выбранное событие, TanStack Query
  дедуплицирует запросы по ключу.
- Выбор события: `?incident=` из URL (`INCIDENT_SEARCH_PARAM`,
  `src/constants/routing.ts`), фолбэк — первое событие списка (сентябрь).
  Параметр пока только читается; писать его начнут лента и переключатель
  май/сентябрь в своих сессиях.
- `src/features/river-scheme/`: `scheme-model.ts` (чистая подготовка данных:
  ordered/unordered по `riverOrder`, `valueShare` = доля от максимума события,
  коридор — passthrough `corridorBounds`), `StationNode` (узел: усечённое имя
  с тултипом, монохромный бар «длина, не цвет», число через `MeasurementValue`),
  `OrderedStations` (вертикальная линия + `CorridorBand` между границами;
  открытый вверх интервал — лента без верхней рамки от первой строки, со
  стрелкой), `UnorderedStations` (группа «порядок не подтверждён»: явная
  оговорка текстом, без линии и ленты, границы коридора — бейджи на створах),
  `RiverScheme` (контейнер: запросы + состояния; `RiverSchemeContent` —
  презентационная часть для дев-превью; пустые станции → `InsufficientData`
  из `unknowns` — кейс Актау уже работает в колонке схемы).
- `src/constants/scheme.ts` — все подписи схемы (осторожные формулировки);
  подзаголовок честен: «вверху — выше по течению» только когда ВСЕ станции
  упорядочены, иначе «порядок створов не подтверждён».
- Дев-превью `/dev/river-scheme`: обе ветки; «условный порядок» = номера строк
  таблицы §5, помечен как разметка для вёрстки, не утверждение о течении.
- Проверено: `typecheck`, `lint`, `build` зелёные, дев-чанков в прод-бандле нет;
  Playwright 1280×720 свет/тьма: сентябрь — группа без ранжирования с бейджем
  границы коридора, май открывается по deep-link `?incident=`, все 7 сентябрьских
  чисел ведут на `#page=22`, майские — без якоря; в консоли только
  запланированные `STUB:`-варны.
- **Контрольная точка 12 ч выполнена:** сентябрьский кейс виден на схеме,
  каждое число открывает источник.
- **Следующая задача (сессия 6): правая панель** — наполнить 6 блоков §13
  реальными данными выбранного события (Вывод, `EvidenceLevelBadge`, факты,
  не подтверждается, неизвестно, источники через `SourceLink`).
- **Блокеры прежние:** вопросы 1 (порядок створов — обе ветки схемы готовы,
  ответ команды включит линию правкой `riverOrder` в seed-data) и 2
  (incident vs investigation).
</details>

<details>
<summary>Сессия 4 (04.08.2026) — common-примитивы + lib/format. Завершена.</summary>

- `src/lib/format.ts` — Intl ru-RU: `formatNumber`/`formatMeasurement` (неразрывный
  пробел перед единицей, максимум 3 знака — вид чисел 1:1 с таблицами §5),
  `formatDate`/`formatDateTime`/`formatMonth`/`formatSampledAt` (фиксированная зона
  `Asia/Atyrau` = +05:00; месячная точность `'2025-09'` не превращается в выдуманный
  день — «сентябрь 2025 г.»).
- `src/lib/source.ts` — `sourceHref` (якорь `#page=N` только для PDF с подтверждённой
  страницей; сентинел `sourcePage = 0` → документ целиком), `hasConfirmedPage`.
- `src/constants/units.ts` — `UNIT_LABELS` (мг/дм³…), `MATRIX_LABELS`.
- `src/components/common/` + barrel: `EvidenceLevelBadge` (палитра §6 живёт только
  здесь, тултип с расшифровкой уровня, `compact`-режим для ленты/схемы),
  `SourceLink` (издатель · стр. · дата · sha256-префикс, когда бэк его посчитает),
  `MeasurementValue` (кликабельное число → документ + страница, критерий §16 п.5;
  тултип: показатель, дата отбора, документ), `InsufficientData` (полноценное
  состояние; строки — в `constants/strings.ts`).
- shadcn добавлены: `badge`, `tooltip`; `TooltipProvider` обёрнут в `main.tsx`.
- Дев-галерея `/dev/primitives` (`src/dev/`) — только в dev-сборке
  (`import.meta.env.DEV` + lazy; проверено: в прод-бандле чанка нет). Данные — seed.
- Проверено: `typecheck`, `lint`, `build` зелёные; Playwright 1280×720 свет/тьма:
  палитра §6 читается в обеих темах, тултипы обеих веток (стр. 22 / «страница
  уточняется»), у майских ссылок якоря нет, консоль чистая.
</details>

<details>
<summary>Сессия 3 (04.08.2026) — каркас трёх колонок + шкала реплея + роутер/провайдеры. Завершена.</summary>

- `src/router.tsx` — createBrowserRouter, пока один маршрут `/` → `App`;
  `/dossier/:id` и search-параметры deep-link добавятся в своих сессиях.
- `src/main.tsx` — ThemeProvider → QueryClientProvider → RouterProvider.
  QueryClient: `staleTime: Infinity`, `retry: false`, `refetchOnWindowFocus: false`
  (офлайн-демо, данные бюллетеней во время показа не меняются).
- `src/App.tsx` — грид `header / три колонки / шкала` на `h-svh`; на `<lg`
  колонки складываются в столбец. Колонки: `SignalFeed` (features/feed),
  `RiverScheme` (features/river-scheme, заготовка узлов + подпись
  «вверху — выше по течению»), `ConclusionPanel` (features/conclusion —
  6 нумерованных блоков строго из `CONCLUSION_SECTIONS`), `ReplayTimeline`
  (features/replay — маркеры на реальных `REPLAY_STEP_OFFSETS_MS`, play отключён),
  `AppHeader` (components/layout). Наполнение — скелетоны, данные не подключались.
- Константы: `panel.ts` (CONCLUSION_SECTIONS §13 — порядок массива = контракт),
  `strings.ts` + APP_NAME/APP_TAGLINE, `replay.ts` + REPLAY_STEP_TYPE_ORDER (§14).
- shadcn добавлены: `skeleton`, `scroll-area`, `separator` (separator пока не
  используется — блоки панели разделены border-t). В сгенерированном
  `scroll-area.tsx` удалён неиспользуемый импорт React — валил `tsc -b`.
- `index.html`: `lang="ru"`, title «Каспийский след».
- Проверено: `typecheck`, `lint`, `build` — зелёные; визуально через Playwright
  на 1280×720 (свет и тьма): все 6 блоков §13 видны без прокрутки, консоль чистая.
</details>

<details>
<summary>Сессия 2 (04.08.2026) — типы + константы + api-слой с заглушками. Завершена.</summary>

- Установлены: @tanstack/react-query, zustand, react-router-dom, @types/geojson (dev).
- `src/types/` — 1:1 из ТЗ §8 + `ReplayStep` из §12 (payload = unknown), barrel `index.ts`.
- `src/constants/` — `api` (API_BASE_URL с фолбэком `/api`, MAX_INCIDENTS_LIMIT),
  `evidence` (EVIDENCE_LEVEL_META по §6), `phenomena` (PHENOMENON_LABELS,
  VERIFICATION_STATUS_META), `replay` (REPLAY_STEP_OFFSETS_MS §12, лейблы шагов),
  `strings` (LEGAL_DISCLAIMER, OBJECT_FOR_REVIEW_LABEL, INSUFFICIENT_DATA_TITLE).
- `src/api/` — `client` (apiGet/apiPost + warnStubOnce), `contracts` (**провизорные**
  формы ответов §12 — вопросы 2/3/6 открыты), `seed-data` (только данные ТЗ
  §5/§7/§10/§14, riverOrder везде null), заглушки `incidents`, `replays`,
  `investigations`, `live-status` — реестр в `docs/stubs.md`.
- `.env.example` (VITE_API_BASE_URL, необязателен), `vite-env.d.ts`;
  в eslint отключён react-refresh/only-export-components для `src/components/ui`.
- ВАЖНО: eslint запускать из `apps/web` (конфиг там), корневой `npm run lint`
  через обёртку может искать конфиг в корне.
</details>

<details>
<summary>Сессия 1 (04.08.2026) — аудит ТЗ, монорепа. Завершена.</summary>

- Созданы файлы памяти: `CLAUDE.md`, `docs/frontend-plan.md`, `docs/decisions.md`, `docs/stubs.md`.
- Монорепа по ТЗ §11 (вопрос 9 закрыт): `apps/web` + npm workspaces, команды из корня.
- `.gitignore`: `.env`/`.env.*` игнорируются, `.env.example` разрешён.
- Установлены React 19, TS strict, Tailwind 4, shadcn на Base UI (base-lyra), @fontsource Inter.
</details>

## Открытые вопросы к команде

Обновлено 04.08.2026 после сессии 13. Готовый к отправке текст —
`docs/questions-to-fullstack.md`; здесь — короткий реестр со сквозной
нумерацией сессий 1–12, чтобы старые записи не потеряли смысл.

**ЗАКРЫТЫ пакетом `packages/contracts` (04.08.2026):** 2 (id ленты = id
расследования), 3 (payload реплея — дискриминированный юнион, совпал с нашим
дословно, включая офсеты), 4 (`IncidentSummary.period` подтверждён), 5 (состав
досье — плоский `Dossier`, поля `rulesetVersion`/`inputHash` в нём же),
6 (`corridorBounds` подтверждён), 7 (Актау — обычный incident, причины отказа
в `unknowns`), 9 (монорепа), 11 (общий пакет = `packages/contracts`, владелец
Full-stack 1), 12 (`PORT=3000`, префикс `api`, CORS на `localhost:5173`,
плюс наш Vite proxy), 13 (секреты в `apps/api/.env`), 14 (`provenance` —
не в `IncidentDetail`, а в `Dossier`).

**ЗАКРЫТЫ веткой `feat/backend-investigation` (05.08.2026):** 1 (порядок створов —
частично: четыре створа из семи, проверенными попарными связями), 8 (майская
страница 24, sha256 обоих бюллетеней, дословные выдержки), 16 (фикстуры мая
и Актау — на уровне golden расчётного ядра).

**ОСТАЛИСЬ ОТКРЫТЫМИ:**

1а. **Хвост вопроса 1.** Где в цепочке стоят «0,5 км выше / 3 км ниже сброса
   осетрового завода» и «посёлок Дамба» относительно городских створов.
   Пока их порядок не подтверждён, они показываются группой без ранжирования —
   это рабочее состояние, а не блокер.
8а. **Якоря PDF.** Проверить, отрабатывает ли `#page=N` на реальных URL
   Казгидромета без принудительного скачивания (теперь якорь ставят и майские
   числа). Фолбэк на случай неудачи: попап с `sourceExcerpt` — выдержки есть.
10. **Цвет значений.** Формула «относительного положения» — предложена
    монохромная нормировка внутри события (длина бара = доля от максимума);
    нужно согласие, а не решение.

**НОВОЕ, появилось из ветки бэкенда:**

15. **Тестовая инфраструктура.** В `apps/web` нет ни `vitest`, ни
    `@playwright/test` — этап F8 не с чего начинать. Ставит Full-stack 1
    общим коммитом (в `apps/api` и `packages/contracts` jest уже есть).
17. **ESM-выход `packages/contracts`.** Пакет собирается в CommonJS; из-за
    этого он почти не поддаётся tree-shaking, и бандл фронта вырос
    с ~523 kB до 884 kB. Плюс dev-сервер требует `optimizeDeps.include`.
18. **`unknowns` в двух формах.** `Investigation.unknowns` — массив строк,
    `Dossier.unknowns` — массив `{code, text}`. Экспорт из сида подставляет
    `code: 'UNSPECIFIED'`, потому что таксономии кодов у нас нет. Свести к одной
    форме или сказать, кто присваивает коды.
19. **`react-router`: 2 high в `npm audit`.** Full-stack 1 оставил решение
    фронту (`audit fix --force` — breaking). Пока не трогаю: обновление
    роутера перед демо рискованнее самой уязвимости в офлайн-приложении.

**НОВОЕ, появилось из ветки расследований (сессия 14):**

20. **Вывод Актау обеднел.** У ядра `L0` + три пробела кодами, но пропала
    формулировка ТЗ §7.6 «есть сообщения и модель волн, но нет поля течений» —
    на экране «недостаточно данных» это была часть «что у нас есть».
    Вернуть в `conclusion` ядра или согласиться, что демо покажет сухую версию?
21. **Эндпоинты ленты и события.** `GET /api/incidents` и `GET /api/incidents/:id`
    не подняты, поэтому три готовых эндпоинта Backend 2 включить не во что:
    `VITE_DATA_MODE=api` без ленты даёт пустой экран. ← **БЛОКЕР F3/F5**.
22. **Порог `verified` у документов.** У бюллетеней настоящий sha256, но
    в выгрузке бэка они по-прежнему `status: 'unverified'` с `fetchedAt: null`.
    Что должно появиться, чтобы документ стал `verified` — время скачивания
    или подпись человека (`requiredHumanReviewers: 2`)?

## Задачи по блокам

### Сессия 1 — аудит ТЗ, план, файлы памяти
- [x] Завершена 04.08.2026.

### Блок 2–24 ч (фронт работает параллельно бэку, ничего не ждёт)
- [x] **Типы + константы + api-слой с заглушками** — сессия 2, 04.08.2026
  - [x] deps: @tanstack/react-query, zustand, react-router-dom (+ @types/geojson);
  - [x] `src/types/` буквально по ТЗ §8;
  - [x] `src/constants/` (api, evidence, phenomena, replay, strings — юр. оговорка);
  - [x] `src/api/` с заглушками (только реальные данные ТЗ §5 + кейс Актау);
  - [x] `.env.example` с `VITE_API_BASE_URL`; чтение с фолбэком — сборка работает без `.env`.
- [x] Каркас трёх колонок + шкала снизу (порядок блоков панели строго по ТЗ §13) — сессия 3, 04.08.2026
- [x] Common-примитивы: `MeasurementValue`, `SourceLink`, `EvidenceLevelBadge`, `InsufficientData` + `lib/format.ts` — сессия 4, 04.08.2026
- [x] Линейная схема реки v1 на сентябрьских данных, числа кликабельны — сессия 5, 04.08.2026
  (обе ветки: линия по `riverOrder` + группа «порядок не подтверждён»; коридор
  из `corridorBounds`, включая открытый вверх; Актау в колонке схемы —
  `InsufficientData`).
- [x] **Контрольная точка 12 ч:** сентябрьский кейс виден на схеме, каждое число открывает источник. ✓ 04.08.2026

### Блок 24–36 ч
- [x] Правая панель: 6 блоков по ТЗ §13 (Вывод → Уровень → Установлено → Не подтверждается → Неизвестно → Источники) — сессия 6, 04.08.2026.
- [x] Состояния L0–L3 через `EVIDENCE_LEVEL_META` (цвета из ТЗ §6: серый/синий/янтарный/зелёный) — бейдж с сессии 4, в панели с расшифровкой с сессии 6.
- [x] Лента слева: сигналы + расследования — сессия 7, 04.08.2026 (клик пишет `?incident=`,
  сигналы выбранного события из detail, уровни компактными бейджами).
- [x] Экран Актау «недостаточно данных» — полноценный, часть демо — сессия 8, 04.08.2026
  (`InsufficientDataScreen` заменяет всю центральную колонку при нуле створов).

### Блок 36–45 ч
- [x] Реплей: шкала, play/пауза, перемотка по шагам, клавиатура (Space, ←/→) — сессия 9, 04.08.2026
  (плеер на `replayStore` + `useReplayPlayback`; панели читают кадр без рефетча).
- [x] Переключение май/сентябрь с ЯВНОЙ визуальной сменой вывода — сессия 10, 04.08.2026
  (`PeriodSwitcher` с уровнями на кнопках + плашка «Вывод изменился» было/стало).
- [x] Печатное досье: маршрут `/dossier/:id`, `@media print`, «Скачать JSON» —
  сессия 12, 04.08.2026 (12 секций роадмапа §21.1; печать проверена настоящим
  PDF в тот же день, не в последний час).

### Переупорядочено обновлённым ТЗ (этапы F0–F9, `docs/context/frontend-role-checklist.md`)
- [x] **Контрактные правки — раньше досье** — сессия 11, 04.08.2026:
  `sourcePage: number | null` вместо сентинела `0`, `sampledPeriod`/`observedPeriod`
  отдельными полями, `verificationStatus: 'conflicting'`, `extractionMode:
  'llm_verified'`, фолбэки `unitLabel`/`matrixLabel`/`phenomenonLabel` на сырую
  строку. Заодно починен скрипт `typecheck` (`tsc -b` вместо no-op `tsc --noEmit`).
- [x] **Печатное досье по новому составу (F4)** — сессия 12, 04.08.2026:
  12 секций §21.1, `provenance` в контракте (вопрос 14), шестая заглушка
  `src/api/export.ts`, печатные стили и проверка PDF.
- [x] **Переезд на `packages/contracts`** — сессия 13, 04.08.2026: типы и Zod-схемы
  из общего пакета, `src/types/*` удалены, сид приведён к схемам и сам ими
  проверяется, сентябрь принят как L1 без коридора.
- [x] **API-слой (F2)** — сессия 13, 04.08.2026: `VITE_DATA_MODE=api|seed` в каждом
  модуле `src/api/*`, проверка ответа схемой на границе сети, `AbortSignal` + timeout,
  нормализованная `ApiRequestError {code,message,requestId}`, фабрика `queryKeys`,
  `staleTime` 60 с для live-status, Vite proxy `/api → :3000`.
- [ ] Подключение реальных эндпоинтов по мере готовности бэка (F3/F5): переключить
  `VITE_DATA_MODE=api`, сверить числа с сидом; статусы в `docs/stubs.md`
  не удалять, а помечать «API готов, seed fallback сохранён».
- [ ] Состояния источников (F6): healthy / degraded / rate limited / failed / cache /
  last success в шапке или поповере; «источник недоступен» ≠ «событий нет».
- [ ] Тесты (F8): unit на store/projection/форматирование/`sourceHref`/уровни и
  E2E Playwright на десятишаговый критический сценарий. Инфраструктуры пока нет.
- [ ] Хвосты F1: drag шкалы реплея, `prefers-reduced-motion`.
- [ ] Проверка на 1280×720 и 1440×900 (проектор): контраст, размер шрифта, три колонки.
- [ ] Данные Мангистау. Мобильная вёрстка — остаточный принцип.
- [ ] Feature freeze (43 ч): полировка, прогон демо офлайн, резервное видео, новых фич нет.

## Порядок выбрасывания при отставании
MapLibre-карта → слой волн Актау → мобильная адаптация → строка live/status → анимации.
**НЕ выбрасывать:** реплей, досье, экран Актау, кликабельные числа, смена вывода май/сентябрь.

## Целевая структура src/

```
src/
  api/            — слой запросов; ЕДИНСТВЕННОЕ место заглушек (client, incidents, investigations, replays, liveStatus)
  types/          — типы 1:1 из ТЗ §8
  constants/      — api, evidence (EVIDENCE_LEVEL_META), phenomena, replay, strings (юр. оговорка)
  lib/format.ts   — числа ru-RU, даты +05:00, мг/дм³
  hooks/          — use-selected-incident-detail (общий выбор события), use-verdict-change (переход между периодами)
  stores/replayStore.ts — Zustand: сценарий, индекс шага, status, позиция парой якорей
  components/ui/  — shadcn (Base UI)
  components/common/ — MeasurementValue, SourceLink, EvidenceLevelBadge, InsufficientData
  features/
    feed/         — SignalFeed, SignalCard
    river-scheme/ — RiverScheme (DOM/flex, решение сессии 5), scheme-model, StationNode, OrderedStations, CorridorBand, UnorderedStations (riverOrder=null)
    conclusion/   — ConclusionPanel + panel-model, StatementList (блоки 3–4), SourcesList
    replay/       — ReplayTimeline (плеер + шкала), useReplayPlayback (таймеры + плейхед), replay-frame (кадр шага и проекция detail)
    comparison/   — PeriodSwitcher, VerdictChange («вывод изменился»), comparison-model
    dossier/      — DossierPage (маршрут + печать + JSON), DossierDocument (12 секций §21.1),
                    dossier-model, SignalTimeline, MeasurementTable, CandidateObjectList, DossierSources
    aktau/        — экран «недостаточно данных»
  App.tsx, main.tsx, router.tsx
```

## Подводные камни (сверх раздела 8 промпта; полный аудит — сессия 1)
- Таймеры реплея: от `performance.now()` с хранимым offset; пауза/seek сбрасывают таймеры;
  цепочка `setTimeout` дрейфует. ← СНЯТО сессией 9: позиция хранится парой якорей,
  таймер один и всегда до абсолютного `offsetMs` следующего шага.
- TanStack Query и реплей не смешивать: панели читают состояние текущего шага из селектора
  replayStore поверх загруженных данных, без рефетча на шаг. ← СНЯТО сессией 9
  (`useReplayFrame` + `projectDetailForReplay`).
- Длинный текст в строке-плеере распирает грид: строке грида нужен `min-w-0`, иначе её
  минимальная ширина считается по содержимому и появляется горизонтальная прокрутка.
  Проверять `scrollWidth > clientWidth` на 1280×720 после каждой правки футера.
- ~~`.prettierrc` расходится с кодовой базой~~ СНЯТО 04.08.2026: разовый прогон
  `prettier --write` по `apps/web` выполнен отдельным коммитом, добавлен
  `.gitattributes` (`* text=auto eol=lf`) — `npm run format` теперь безопасен,
  и конфликта концов строк на разных ОС не будет.
- Печать из тёмной темы: браузер не печатает фоны, поэтому светлый текст ложится белым
  по белому. Лечится в `@media print` подменой переменных тем — но только переменных:
  утилиты с `dark:`-вариантом (палитра уровней) требуют отдельной print-пары. ← СНЯТО
  сессией 12 для `EvidenceLevelBadge`; при добавлении новых цветных классов проверять.
- Base UI `Button render={<Link/>}` рендерит `<a role="button" tabindex="0">`: ссылка
  объявляется кнопкой, ломается «открыть в новой вкладке». Для навигации — `Link`
  с `buttonVariants(...)`, компонент `Button` только для настоящих кнопок.
- PDF-якоря `#page=N` зависят от заголовков сервера Казгидромета — проверить реальные URL;
  фолбэк: попап с `sourceExcerpt` + номер страницы текстом.
- Длинные подписи створов («0,5 км ниже сброса ГКП "Атырау су арнасы"») — сокращение + тултип;
  тултипы Base UI над SVG проверить в первый день схемы. ← СНЯТО сессией 5:
  схема — DOM/flex, не SVG. Сессия 12: тултип с полным именем убран — он дословно
  повторял видимую подпись; осталось усечение, полные имена есть в панели и досье.
- Тултип, дублирующий видимый текст, — шум, а не подсказка. И открываться он должен
  вниз: сверху попап накрывает строку, на которую наведён курсор (`side="bottom"`
  по умолчанию в `components/ui/tooltip.tsx`).
- Кнопка в шапке колонки выше строки заголовка и растягивает шапку: линия `border-b`
  разъезжается с соседними колонками. Лечится `-my-1` на кнопке, а не изменением
  `py` — высота шапок (41 px) общая для всех трёх колонок.
- Deep-link: выбранное расследование и месяц в URL (защита: открыть экран закладкой, пережить F5).
- Печать проверять в блоке 36–45, не в последний час.
- Всё офлайн: никаких CDN (шрифт уже локален; тайлы карт — аргумент против MapLibre).
- React 19 / Vite 8 / TS 6 / ESLint 10 — примеры из интернета устарели в обе стороны,
  сверяться с установленными версиями.
- Зелёная проверка может ничего не проверять: `tsc --noEmit` на solution-конфиге
  (`"files": []` + `references`) молча компилирует ноль файлов. Нужен режим сборки
  (`tsc -b`). Сомневаешься в команде — сломай код намеренно и убедись, что она падает.
- `packages/contracts` собирается в CommonJS. Прод-сборка это переваривает, а dev-сервер
  Vite линкованный workspace-пакет не предбандливает и отдаёт как есть — именованные
  импорты падают с «does not provide an export named ...». Лечится
  `optimizeDeps.include`. Мораль: прод-сборка ≠ проверка dev-режима, гонять надо оба.
- Схемы пакета — не только для сети: сид проходит через те же `parse`, иначе офлайн-режим
  тихо разъедется с контрактом, и узнаем мы об этом в момент интеграции.
- Правки бэка приходят не только контрактом, но и **оценками**: сентябрьский уровень
  и состав утверждений — его данные. Прежде чем «чинить» разошедшийся сид, надо понять,
  что именно бэк утверждает и почему (его отчёт `docs/backend-platform/*`).
