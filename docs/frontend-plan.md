# План фронтенда «Каспийский след»

> **04.08.2026 пришло обновлённое ТЗ** от бэкендера: `docs/caspian-trace-development-roadmap.md`
> (общий план команды) и `docs/caspian-trace-role-frontend.md` (план моей роли, этапы F0–F9).
> Разбор — в `docs/context/`: приоритет документов (`sources-of-truth.md`), что закрыто
> и что изменилось (`2026-08-04-updated-spec.md`), контракт одной страницей
> (`frontend-api-contract.md`), сверка этапов (`frontend-role-checklist.md`).
> Читать эту папку в начале сессии, если задача касается контракта, досье или интеграции.

## Состояние на конец последней сессии

**Сессия 10 (04.08.2026) — переключение май/сентябрь со сменой вывода. Завершена.**

- **В провизорный контракт добавлено поле `IncidentSummary.period`** (ISO до
  месяца, `null` у Актау): без него список событий отличал май от сентября
  только разбором заголовка. Значения в seed реальные — те же месяцы, что
  у измерений §5. Ответ команды по вопросу 4 правит `contracts.ts` + `seed-data.ts`.
- `features/comparison/comparison-model.ts` — сопоставимые события выводятся
  из полей API (одна область + один показатель + известный период), связь
  май↔сентябрь не выдумывается; `areComparable` — та же проверка для пары.
- `PeriodSwitcher` (над схемой): периоды участка кнопками, уровень стоит
  прямо на кнопке — разница L2/L3 видна ещё до клика, и переключение не
  выглядит сменой фильтра. Пишет тот же `?incident=`, что и лента.
- `VerdictChange` + `hooks/use-verdict-change.ts` — плашка «Вывод изменился»
  при переходе между сопоставимыми периодами: было → стало с периодом,
  уровнем и дословными выводами обоих событий, плюс объяснение §14, что
  вывод следует за фактами измерений, а не переоценивает объекты.
  Живёт МЕЖДУ шапкой панели и прокручиваемой областью — не теряется, если
  панель прокручена. Закрывается крестиком, при новом переходе появляется
  снова. Предыдущий выбор берётся сравнением с прошлым рендером (штатный
  приём React), данные — из кэша TanStack Query, без единого нового запроса.
- Во время реплея плашка не показывается: там своя хронология.
- Дев-превью `/dev/comparison`: переключатель, кейс «пары нет» (Актау),
  оба направления перехода.
- Проверено: `typecheck`, `lint`, `build` зелёные, дев-чанков в прод-бандле нет;
  Playwright 1280×720 свет/тьма: переключение в обе стороны меняет все три
  колонки и показывает плашку L2↔L3, «скрыть» работает, у Актау нет ни
  переключателя, ни плашки; в консоли только `STUB:`-варны.
- **Следующая задача (сессия 11): печатное досье** — маршрут `/dossier/:id`,
  `@media print`, юр. оговорка `LEGAL_DISCLAIMER`; print preview проверить
  сразу, не в последний час (блок 36–45 ч).
- **Блокеры прежние:** вопросы 1, 2, 3; вопрос 4 частично закрыт провизорным
  `period` — команде подтвердить или заменить настоящей связью расследований.

<details>
<summary>Сессия 9 (04.08.2026) — реплей: плеер, шкала, клавиатура. Завершена.</summary>

- `src/stores/replayStore.ts` (единственная зона Zustand): сценарий, индекс шага,
  `status` (paused/playing/finished) и позиция **парой якорей** —
  `anchorPositionMs` (зафиксирована последним play/pause/seek) +
  `anchorTimeMs` (`performance.now()` момента старта, null = стоим). Текущая
  позиция всегда вычисляется `getReplayPositionMs()`, поэтому пауза и перемотка
  не копят дрейф. Действия: `start`/`play`/`pause`/`seekToStep`/`advanceToStep`/`exit`.
- `src/features/replay/useReplayPlayback.ts`: `useReplayPlayback` — один
  `setTimeout` до границы следующего шага, задержка каждый раз считается от
  абсолютного `offsetMs` (не цепочка относительных таймеров);
  `useReplayPositionMs` — плейхед через `requestAnimationFrame`, живёт вне
  стора, чтобы 60 fps не дёргали подписчиков.
- `src/features/replay/replay-frame.ts` — «кадр» реплея: что уже показано к
  текущему шагу (`visibleMeasurementIds`, `visibleSourceDocumentIds`,
  `inferenceReached`, `conclusionText`, `evidenceLevel` из payload шага).
  `useReplayFrame(incidentId)` отдаёт кадр только для СВОЕГО события,
  `projectDetailForReplay` — проекция detail на кадр.
- `ReplayTimeline` переписан в плеер: play/пауза/перезапуск, кнопка выхода,
  кликабельные маркеры (клик без запущенного реплея = ручной режим на этом
  шаге), плейхед и залитая часть шкалы, строка текущего шага с дословным
  текстом payload и компактным `EvidenceLevelBadge`; клавиатура — пробел
  (пуск/пауза), ←/→ (по шагам), обработчик игнорирует поля ввода и не
  перехватывает пробел на кнопках/ссылках.
- Панели читают кадр **селектором поверх загруженных данных, без рефетча**:
  схема показывает только «загруженные» шагами значения и коридор (он —
  результат шага inference), панель гейтит блоки 1/3/4/5 строкой
  `REPLAY_PENDING`, уровень и вывод берёт из payload шага.
- Сценарий читается через `replayScenarioQueryOptions` (POST, но сценарий
  неизменяем → query): недоступный сценарий заранее гасит кнопку play.
  Смена события гасит активный реплей (защита от рассинхрона колонок).
- **Починена регрессия вёрстки:** футер как строка грида получал минимальную
  ширину по содержимому, и длинный текст вывода растягивал весь экран вбок
  (горизонтальная прокрутка на 1280×720). Лечится `min-w-0` на `<footer>`;
  строка шага — блок с `truncate`, а не flex.
- Проверено: `typecheck`, `lint`, `build` зелёные, дев-чанков в прод-бандле нет;
  Playwright 1280×720 свет/тьма: все 5 шагов по §14 (L0 → L1 → L1+значения →
  L2+коридор+факты → вывод), пауза держит позицию и продолжает с неё же,
  пробел/стрелки работают, май и Актау без сценария дают отключённое
  управление с осторожной формулировкой, выход возвращает полные данные;
  в консоли только `STUB:`-варны.
- **Следующая задача (сессия 10): переключение май/сентябрь** — выполнена.
- **Блокеры прежние:** вопросы 1 и 2; вопрос 3 (payload реплея) теперь
  особенно важен — весь плеер построен на провизорном `TypedReplayStep`.

<details>
<summary>Сессия 8 (04.08.2026) — экран Актау «недостаточно данных». Завершена.</summary>

- `src/features/aktau/InsufficientDataScreen.tsx` — полноэкранное состояние
  центральной колонки для события без единого створа: область
  (`REGION_LABELS`), заголовок события, `EvidenceLevelBadge` с расшифровкой
  в тултипе, дословный осторожный вывод с бэка цитатой и `InsufficientData`
  с причинами (unknowns). Компонент generic: кейс Актау не хардкодится,
  любое событие без геометрии получает тот же экран.
- `RiverSchemeContent` при `stations.length === 0` рендерит экран вместо
  заголовка «Линейная схема реки» + голого бокса: заголовок про реку для
  прибрежного кейса был неправдой, а полноэкранное состояние читается как
  осознанный отказ (§13: жюри должно увидеть, что система не генерирует
  убедительный ответ без данных). Заодно исправлено центрирование при
  переполнении (min-h-full вместо items-center на overflow-контейнере).
- Отдельная галерея не понадобилась: кадр «Актау — недостаточно данных»
  в `/dev/river-scheme` показывает новый экран автоматически.
- Проверено: `typecheck`, `lint`, `build` зелёные; Playwright 1280×720
  свет/тьма: экран Актау по deep-link, сентябрьская схема без регрессий,
  кадр галереи скроллится в рамке; в консоли только `STUB:`-варны.
- **Следующая задача (сессия 9): реплей** — выполнена.
- **Блокеры прежние:** вопросы 1 и 2; для типизации payload реплея открыт
  вопрос 3 (провизорный `TypedReplayStep` уже есть).

<details>
<summary>Сессия 7 (04.08.2026) — лента сигналов и расследований. Завершена.</summary>

- `src/features/feed/`: `SignalFeed` (контейнер: список событий + запись
  выбора; `SignalFeedContent` — презентационная часть для дев-превью;
  `IncidentCard` — карточка события: заголовок, показатель + область,
  компактный `EvidenceLevelBadge`; «растянутая кнопка» after:inset-0 —
  бейдж с тултипом и ссылки сигналов остаются настоящими интерактивами),
  `SignalCard` (публичный сигнал: явление + статус проверки из
  `VERIFICATION_STATUS_META`, дословная цитата, локация и хронология
  наблюдалось/сообщено, `SourceLink` на первоисточник — §16 п.5 и для сигналов).
- Клик по карточке пишет `?incident=` через `setSearchParams` (push —
  история выбора листается «назад»); хук `use-selected-incident-detail`
  теперь возвращает и `selectedIncidentId` — подсветка карточки и данные
  схемы/панели идут из одного источника истины; `aria-current` на выбранной.
- Сигналы разворачиваются под карточкой ВЫБРАННОГО события из его detail
  (списковый эндпоинт сигналы не отдаёт — вопрос 2; detail уже в кэше
  TanStack Query для схемы и панели). Пустое состояние — осторожная
  формулировка в `constants/feed.ts`; `REGION_LABELS` — `constants/regions.ts`.
- Дев-превью `/dev/feed`: три состояния выбора (сентябрь с сигналом,
  май и Актау без), клики работают на локальном useState, URL не трогают.
- Проверено: `typecheck`, `lint`, `build` зелёные, дев-чанков в прод-бандле
  нет; Playwright 1280×720 свет/тьма: клик по маю переключает все три колонки
  разом, deep-link `?incident=` переживает прямую загрузку, `aria-current`
  проставлен; в консоли только `STUB:`-варны.
- **Следующая задача (сессия 8): экран Актау «недостаточно данных»** —
  состояние уже работает во всех трёх колонках, довести до полноценного
  демо-экрана (§7.6/§13) и закрыть блок 24–36 ч; после него — реплей.
- **Блокеры прежние:** вопросы 1 и 2.
</details>

<details>
<summary>Сессия 6 (04.08.2026) — правая панель §13 на данных выбранного события. Завершена.</summary>

- `src/hooks/use-selected-incident-detail.ts` — общий хук выбора события
  (`?incident=` → первое в списке); `RiverScheme` переведён на него, панель
  и схема гарантированно показывают одно и то же событие (запросы
  дедуплицируются TanStack Query по ключу).
- `src/features/conclusion/`: `panel-model.ts` (утверждениям разворачиваются
  измерения-основания со створом и документом), `StatementList` (один компонент
  на блоки 3–4 — текст утверждения с бэка дословно + кликабельные значения под
  ним, §16 п.5 работает и в панели), `SourcesList` (блок 6, страница — из
  ссылающегося измерения: сентябрь «стр. 22», май скрыт сентинелом 0),
  `ConclusionPanel` (контейнер + `ConclusionPanelContent` для дев-превью;
  секции строго из `CONCLUSION_SECTIONS`).
- Блок 2 — `EvidenceLevelBadge` + видимая расшифровка уровня из
  `EVIDENCE_LEVEL_META` (§13 «с расшифровкой», не только в тултипе).
- Пустые состояния — осторожные формулировки в `constants/panel.ts`
  («Ни одна конкретная версия пока не исключена фактами» — состояние данных,
  не оценка невиновности); Актау в панели полностью рабочий: L0 + пустые
  блоки + unknowns.
- `DATA_LOAD_ERROR` перенесён в `constants/strings.ts` (общий для колонок),
  `SCHEME_LOAD_ERROR` удалён.
- Дев-превью `/dev/conclusion`: сентябрь (L2, исключённая версия),
  май (L3), Актау (L0, пустые состояния) в ширине реальной колонки.
- Проверено: `typecheck`, `lint`, `build` зелёные; Playwright 1280×720
  свет/тьма: главный экран (схема + панель на одном событии), галерея панели;
  в консоли только `STUB:`-варны.
</details>

<details>
<summary>Сессия 5 (04.08.2026) — линейная схема реки v1. Завершена.</summary>

- `src/api/queries.ts` — `queryOptions` списка событий и деталей события; все
  колонки главного экрана читают одно выбранное событие, TanStack Query
  дедуплицирует запросы по ключу.
- Выбор события: `?incident=` из URL (`INCIDENT_SEARCH_PARAM`,
  `src/constants/routing.ts`), фолбэк — первое событие списка (сентябрь).
  Параметр пока только читается; писать его начнут лента и переключатель
  май/сентябрь в своих сессиях.
- `src/features/river-scheme/`: `scheme-model.ts` (чистая подготовка данных:
  ordered/unordered по `riverOrder`, `valueShare` = доля от максимума события,
  коридор — passthrough `corridorBounds`), `StationNode` (узел: усечённое имя
  с тултипом, монохромный бар «длина, не цвет», число через `MeasurementValue`),
  `OrderedStations` (вертикальная линия + `CorridorBand` между границами;
  открытый вверх интервал — лента без верхней рамки от первой строки, со
  стрелкой), `UnorderedStations` (группа «порядок не подтверждён»: явная
  оговорка текстом, без линии и ленты, границы коридора — бейджи на створах),
  `RiverScheme` (контейнер: запросы + состояния; `RiverSchemeContent` —
  презентационная часть для дев-превью; пустые станции → `InsufficientData`
  из `unknowns` — кейс Актау уже работает в колонке схемы).
- `src/constants/scheme.ts` — все подписи схемы (осторожные формулировки);
  подзаголовок честен: «вверху — выше по течению» только когда ВСЕ станции
  упорядочены, иначе «порядок створов не подтверждён».
- Дев-превью `/dev/river-scheme`: обе ветки; «условный порядок» = номера строк
  таблицы §5, помечен как разметка для вёрстки, не утверждение о течении.
- Проверено: `typecheck`, `lint`, `build` зелёные, дев-чанков в прод-бандле нет;
  Playwright 1280×720 свет/тьма: сентябрь — группа без ранжирования с бейджем
  границы коридора, май открывается по deep-link `?incident=`, все 7 сентябрьских
  чисел ведут на `#page=22`, майские — без якоря; в консоли только
  запланированные `STUB:`-варны.
- **Контрольная точка 12 ч выполнена:** сентябрьский кейс виден на схеме,
  каждое число открывает источник.
- **Следующая задача (сессия 6): правая панель** — наполнить 6 блоков §13
  реальными данными выбранного события (Вывод, `EvidenceLevelBadge`, факты,
  не подтверждается, неизвестно, источники через `SourceLink`).
- **Блокеры прежние:** вопросы 1 (порядок створов — обе ветки схемы готовы,
  ответ команды включит линию правкой `riverOrder` в seed-data) и 2
  (incident vs investigation).
</details>

<details>
<summary>Сессия 4 (04.08.2026) — common-примитивы + lib/format. Завершена.</summary>

- `src/lib/format.ts` — Intl ru-RU: `formatNumber`/`formatMeasurement` (неразрывный
  пробел перед единицей, максимум 3 знака — вид чисел 1:1 с таблицами §5),
  `formatDate`/`formatDateTime`/`formatMonth`/`formatSampledAt` (фиксированная зона
  `Asia/Atyrau` = +05:00; месячная точность `'2025-09'` не превращается в выдуманный
  день — «сентябрь 2025 г.»).
- `src/lib/source.ts` — `sourceHref` (якорь `#page=N` только для PDF с подтверждённой
  страницей; сентинел `sourcePage = 0` → документ целиком), `hasConfirmedPage`.
- `src/constants/units.ts` — `UNIT_LABELS` (мг/дм³…), `MATRIX_LABELS`.
- `src/components/common/` + barrel: `EvidenceLevelBadge` (палитра §6 живёт только
  здесь, тултип с расшифровкой уровня, `compact`-режим для ленты/схемы),
  `SourceLink` (издатель · стр. · дата · sha256-префикс, когда бэк его посчитает),
  `MeasurementValue` (кликабельное число → документ + страница, критерий §16 п.5;
  тултип: показатель, дата отбора, документ), `InsufficientData` (полноценное
  состояние; строки — в `constants/strings.ts`).
- shadcn добавлены: `badge`, `tooltip`; `TooltipProvider` обёрнут в `main.tsx`.
- Дев-галерея `/dev/primitives` (`src/dev/`) — только в dev-сборке
  (`import.meta.env.DEV` + lazy; проверено: в прод-бандле чанка нет). Данные — seed.
- Проверено: `typecheck`, `lint`, `build` зелёные; Playwright 1280×720 свет/тьма:
  палитра §6 читается в обеих темах, тултипы обеих веток (стр. 22 / «страница
  уточняется»), у майских ссылок якоря нет, консоль чистая.
</details>

<details>
<summary>Сессия 3 (04.08.2026) — каркас трёх колонок + шкала реплея + роутер/провайдеры. Завершена.</summary>

- `src/router.tsx` — createBrowserRouter, пока один маршрут `/` → `App`;
  `/dossier/:id` и search-параметры deep-link добавятся в своих сессиях.
- `src/main.tsx` — ThemeProvider → QueryClientProvider → RouterProvider.
  QueryClient: `staleTime: Infinity`, `retry: false`, `refetchOnWindowFocus: false`
  (офлайн-демо, данные бюллетеней во время показа не меняются).
- `src/App.tsx` — грид `header / три колонки / шкала` на `h-svh`; на `<lg`
  колонки складываются в столбец. Колонки: `SignalFeed` (features/feed),
  `RiverScheme` (features/river-scheme, заготовка узлов + подпись
  «вверху — выше по течению»), `ConclusionPanel` (features/conclusion —
  6 нумерованных блоков строго из `CONCLUSION_SECTIONS`), `ReplayTimeline`
  (features/replay — маркеры на реальных `REPLAY_STEP_OFFSETS_MS`, play отключён),
  `AppHeader` (components/layout). Наполнение — скелетоны, данные не подключались.
- Константы: `panel.ts` (CONCLUSION_SECTIONS §13 — порядок массива = контракт),
  `strings.ts` + APP_NAME/APP_TAGLINE, `replay.ts` + REPLAY_STEP_TYPE_ORDER (§14).
- shadcn добавлены: `skeleton`, `scroll-area`, `separator` (separator пока не
  используется — блоки панели разделены border-t). В сгенерированном
  `scroll-area.tsx` удалён неиспользуемый импорт React — валил `tsc -b`.
- `index.html`: `lang="ru"`, title «Каспийский след».
- Проверено: `typecheck`, `lint`, `build` — зелёные; визуально через Playwright
  на 1280×720 (свет и тьма): все 6 блоков §13 видны без прокрутки, консоль чистая.
</details>

<details>
<summary>Сессия 2 (04.08.2026) — типы + константы + api-слой с заглушками. Завершена.</summary>

- Установлены: @tanstack/react-query, zustand, react-router-dom, @types/geojson (dev).
- `src/types/` — 1:1 из ТЗ §8 + `ReplayStep` из §12 (payload = unknown), barrel `index.ts`.
- `src/constants/` — `api` (API_BASE_URL с фолбэком `/api`, MAX_INCIDENTS_LIMIT),
  `evidence` (EVIDENCE_LEVEL_META по §6), `phenomena` (PHENOMENON_LABELS,
  VERIFICATION_STATUS_META), `replay` (REPLAY_STEP_OFFSETS_MS §12, лейблы шагов),
  `strings` (LEGAL_DISCLAIMER, OBJECT_FOR_REVIEW_LABEL, INSUFFICIENT_DATA_TITLE).
- `src/api/` — `client` (apiGet/apiPost + warnStubOnce), `contracts` (**провизорные**
  формы ответов §12 — вопросы 2/3/6 открыты), `seed-data` (только данные ТЗ
  §5/§7/§10/§14, riverOrder везде null), заглушки `incidents`, `replays`,
  `investigations`, `live-status` — реестр в `docs/stubs.md`.
- `.env.example` (VITE_API_BASE_URL, необязателен), `vite-env.d.ts`;
  в eslint отключён react-refresh/only-export-components для `src/components/ui`.
- ВАЖНО: eslint запускать из `apps/web` (конфиг там), корневой `npm run lint`
  через обёртку может искать конфиг в корне.
</details>

<details>
<summary>Сессия 1 (04.08.2026) — аудит ТЗ, монорепа. Завершена.</summary>

- Созданы файлы памяти: `CLAUDE.md`, `docs/frontend-plan.md`, `docs/decisions.md`, `docs/stubs.md`.
- Монорепа по ТЗ §11 (вопрос 9 закрыт): `apps/web` + npm workspaces, команды из корня.
- `.gitignore`: `.env`/`.env.*` игнорируются, `.env.example` разрешён.
- Установлены React 19, TS strict, Tailwind 4, shadcn на Base UI (base-lyra), @fontsource Inter.
</details>

## Открытые вопросы к команде

Обновлено 04.08.2026 по присланному ТЗ: закрытые вопросы помечены, разбор
ответов — `docs/context/2026-08-04-updated-spec.md` §1.

**ЗАКРЫТЫ обновлённым ТЗ:** 2 (id ленты = id расследования, текущий
`IncidentDetail` утверждён как MVP-контракт), 3 (payload — дискриминированный
юнион; offsets остаются наши), 5 (бэк делает `/export?format=html|json`,
печатный маршрут всё равно наш, от бэка берём JSON), 6 (`corridorBounds`
подтверждён), 7 (Актау — обычный incident без станций), 8 (сентинел `0` отменён:
неизвестная страница — `null`), 12 (Vite proxy `/api → :3000`, `VITE_DATA_MODE`),
13 (секреты только в `apps/api/.env`).

**ОСТАЛИСЬ ОТКРЫТЫМИ:**

1. **Порядок створов:** утверждённый сверху-вниз `riverOrder` для 7 сентябрьских и 2 майских
   точек; где в порядке «посёлок Дамба» и «1 км выше/ниже Атырау» относительно точек
   «Атырау су арнасы». ← БЛОКЕР линейной схемы
2. **Incident vs Investigation:** одна сущность или две? Какой эндпоинт отдаёт ленту слева
   и какой — полные данные правой панели? Будет ли `GET /api/investigations/:id`? ← БЛОКЕР
   фиксации контракта. Провизорный вариант зафиксирован в `src/api/contracts.ts`
   (IncidentSummary/IncidentDetail: investigation + signals + measurements + stations
   + candidateObjects + sourceDocuments) — команде подтвердить или поправить.
3. **Реплей:** `:id` в `POST /api/replays/:id/start` — чей? Пример JSON полного сентябрьского
   сценария со всеми 5 типами шагов — для типизации `payload` (дискриминированный юнион).
   Провизорный юнион — `TypedReplayStep` в `src/api/contracts.ts`.
4. **Май/сентябрь: БЛОКЕР, обострился.** Как в данных связаны два расследования одного
   участка — признак для UI сравнения (главный «вау»-момент демо). Сессия 10 добавила
   провизорное `IncidentSummary.period` (ISO до месяца), сопоставимость выводится из
   `region + indicator + period !== null`. **В sample JSON `GET /api/incidents`
   обновлённого ТЗ (роадмап §9.1) поля `period` нет.** Без него `PeriodSwitcher`
   и плашка «Вывод изменился» останутся без данных в API-режиме. Просить Full-stack 1
   добавить `period` в ответ списка либо дать настоящий признак участка (`siteId`).
5. **Экспорт:** досье печатает фронт из тех же данных, бэковский `/export` не используем — подтвердить.
6. **Коридор:** добавить в контракт пару `stationId` границ (GeoJSON для линейной схемы недостаточен;
   можно в `properties` GeoJSON-фичи). Провизорно — `corridorBounds` в `IncidentDetail`
   (`upstreamStationId: null` = интервал открыт вверх, кейс сентября).
7. **Актау:** формат кейса «недостаточно данных» — incident с `region=mangystau`? причины отказа
   в `unknowns`? кто их формирует?
8. **Страницы PDF:** подтвердить `source_pages` майских значений; проверить, открываются ли URL
   Казгидромета с якорем `#page=N` без принудительного скачивания. В заглушке майская
   `sourcePage = 0` (сентинел «не подтверждено») — заменить после проверки.
9. ~~**Монорепа:** переезд в `apps/web` сейчас или отказ.~~ РЕШЕНО 04.08: переезд выполнен,
   структура по ТЗ §11; бэкендерам — `git pull` до первого файла, их зона `apps/api` и `packages/*`.
10. **Цвет значений:** формула «относительного положения» (моё предложение: монохромная
    min–max нормировка внутри события).
11. **packages/shared:** когда появится, кто владелец.
12. **Dev-окружение:** порт/URL API (CORS или Vite proxy)? Язык UI — только русский?
13. **Секреты бэка:** репо публичный — ключи Gemini/Groq только в `apps/api/.env`
    (в git только `.env.example`); проверить коммиты до пуша. Демо-режим `verified_seed`
    по ТЗ работает без ключей.

## Задачи по блокам

### Сессия 1 — аудит ТЗ, план, файлы памяти
- [x] Завершена 04.08.2026.

### Блок 2–24 ч (фронт работает параллельно бэку, ничего не ждёт)
- [x] **Типы + константы + api-слой с заглушками** — сессия 2, 04.08.2026
  - [x] deps: @tanstack/react-query, zustand, react-router-dom (+ @types/geojson);
  - [x] `src/types/` буквально по ТЗ §8;
  - [x] `src/constants/` (api, evidence, phenomena, replay, strings — юр. оговорка);
  - [x] `src/api/` с заглушками (только реальные данные ТЗ §5 + кейс Актау);
  - [x] `.env.example` с `VITE_API_BASE_URL`; чтение с фолбэком — сборка работает без `.env`.
- [x] Каркас трёх колонок + шкала снизу (порядок блоков панели строго по ТЗ §13) — сессия 3, 04.08.2026
- [x] Common-примитивы: `MeasurementValue`, `SourceLink`, `EvidenceLevelBadge`, `InsufficientData` + `lib/format.ts` — сессия 4, 04.08.2026
- [x] Линейная схема реки v1 на сентябрьских данных, числа кликабельны — сессия 5, 04.08.2026
  (обе ветки: линия по `riverOrder` + группа «порядок не подтверждён»; коридор
  из `corridorBounds`, включая открытый вверх; Актау в колонке схемы —
  `InsufficientData`).
- [x] **Контрольная точка 12 ч:** сентябрьский кейс виден на схеме, каждое число открывает источник. ✓ 04.08.2026

### Блок 24–36 ч
- [x] Правая панель: 6 блоков по ТЗ §13 (Вывод → Уровень → Установлено → Не подтверждается → Неизвестно → Источники) — сессия 6, 04.08.2026.
- [x] Состояния L0–L3 через `EVIDENCE_LEVEL_META` (цвета из ТЗ §6: серый/синий/янтарный/зелёный) — бейдж с сессии 4, в панели с расшифровкой с сессии 6.
- [x] Лента слева: сигналы + расследования — сессия 7, 04.08.2026 (клик пишет `?incident=`,
  сигналы выбранного события из detail, уровни компактными бейджами).
- [x] Экран Актау «недостаточно данных» — полноценный, часть демо — сессия 8, 04.08.2026
  (`InsufficientDataScreen` заменяет всю центральную колонку при нуле створов).

### Блок 36–45 ч
- [x] Реплей: шкала, play/пауза, перемотка по шагам, клавиатура (Space, ←/→) — сессия 9, 04.08.2026
  (плеер на `replayStore` + `useReplayPlayback`; панели читают кадр без рефетча).
- [x] Переключение май/сентябрь с ЯВНОЙ визуальной сменой вывода — сессия 10, 04.08.2026
  (`PeriodSwitcher` с уровнями на кнопках + плашка «Вывод изменился» было/стало).
- [ ] Печатное досье: маршрут `/dossier/:id`, `@media print`; print preview проверить сразу.
  Состав секций пересмотрен обновлённым ТЗ — теперь 12 пунктов (роадмап §21.1), добавились
  хронология сигналов, объекты для проверки, `rulesetVersion`/`inputHash` и «Скачать JSON».

### Переупорядочено обновлённым ТЗ (этапы F0–F9, `docs/context/frontend-role-checklist.md`)
- [ ] **Контрактные правки — раньше досье:** `sourcePage: number | null` вместо сентинела `0`,
  `sampledPeriod` отдельным полем от `sampledAt`, `verificationStatus: 'conflicting'`,
  фолбэки `UNIT_LABELS`/`MATRIX_LABELS` на сырую строку. Иначе досье пишется под
  отменённый контракт.
- [ ] Печатное досье по новому составу (F4).
- [ ] API-слой (F2): `VITE_DATA_MODE=api|seed` в каждом модуле `src/api/*`, Zod-валидация
  на границе сети, `AbortSignal` + timeout, нормализованная ошибка `{code,message,requestId}`,
  фабрика query keys, `staleTime` 60 с для live-status.
- [ ] Подключение реальных эндпоинтов по мере готовности бэка; статусы в `docs/stubs.md`
  не удалять, а помечать «API готов, seed fallback сохранён».
- [ ] Состояния источников (F6): healthy / degraded / rate limited / failed / cache /
  last success в шапке или поповере; «источник недоступен» ≠ «событий нет».
- [ ] Тесты (F8): unit на store/projection/форматирование/`sourceHref`/уровни и
  E2E Playwright на десятишаговый критический сценарий. Инфраструктуры пока нет.
- [ ] Хвосты F1: drag шкалы реплея, `prefers-reduced-motion`.
- [ ] Проверка на 1280×720 и 1440×900 (проектор): контраст, размер шрифта, три колонки.
- [ ] Данные Мангистау. Мобильная вёрстка — остаточный принцип.
- [ ] Feature freeze (43 ч): полировка, прогон демо офлайн, резервное видео, новых фич нет.

## Порядок выбрасывания при отставании
MapLibre-карта → слой волн Актау → мобильная адаптация → строка live/status → анимации.
**НЕ выбрасывать:** реплей, досье, экран Актау, кликабельные числа, смена вывода май/сентябрь.

## Целевая структура src/

```
src/
  api/            — слой запросов; ЕДИНСТВЕННОЕ место заглушек (client, incidents, investigations, replays, liveStatus)
  types/          — типы 1:1 из ТЗ §8
  constants/      — api, evidence (EVIDENCE_LEVEL_META), phenomena, replay, strings (юр. оговорка)
  lib/format.ts   — числа ru-RU, даты +05:00, мг/дм³
  hooks/          — use-selected-incident-detail (общий выбор события), use-verdict-change (переход между периодами)
  stores/replayStore.ts — Zustand: сценарий, индекс шага, status, позиция парой якорей
  components/ui/  — shadcn (Base UI)
  components/common/ — MeasurementValue, SourceLink, EvidenceLevelBadge, InsufficientData
  features/
    feed/         — SignalFeed, SignalCard
    river-scheme/ — RiverScheme (DOM/flex, решение сессии 5), scheme-model, StationNode, OrderedStations, CorridorBand, UnorderedStations (riverOrder=null)
    conclusion/   — ConclusionPanel + panel-model, StatementList (блоки 3–4), SourcesList
    replay/       — ReplayTimeline (плеер + шкала), useReplayPlayback (таймеры + плейхед), replay-frame (кадр шага и проекция detail)
    comparison/   — PeriodSwitcher, VerdictChange («вывод изменился»), comparison-model
    dossier/      — печатный маршрут
    aktau/        — экран «недостаточно данных»
  App.tsx, main.tsx, router.tsx
```

## Подводные камни (сверх раздела 8 промпта; полный аудит — сессия 1)
- Таймеры реплея: от `performance.now()` с хранимым offset; пауза/seek сбрасывают таймеры;
  цепочка `setTimeout` дрейфует. ← СНЯТО сессией 9: позиция хранится парой якорей,
  таймер один и всегда до абсолютного `offsetMs` следующего шага.
- TanStack Query и реплей не смешивать: панели читают состояние текущего шага из селектора
  replayStore поверх загруженных данных, без рефетча на шаг. ← СНЯТО сессией 9
  (`useReplayFrame` + `projectDetailForReplay`).
- Длинный текст в строке-плеере распирает грид: строке грида нужен `min-w-0`, иначе её
  минимальная ширина считается по содержимому и появляется горизонтальная прокрутка.
  Проверять `scrollWidth > clientWidth` на 1280×720 после каждой правки футера.
- ~~`.prettierrc` расходится с кодовой базой~~ СНЯТО 04.08.2026: разовый прогон
  `prettier --write` по `apps/web` выполнен отдельным коммитом, добавлен
  `.gitattributes` (`* text=auto eol=lf`) — `npm run format` теперь безопасен,
  и конфликта концов строк на разных ОС не будет.
- PDF-якоря `#page=N` зависят от заголовков сервера Казгидромета — проверить реальные URL;
  фолбэк: попап с `sourceExcerpt` + номер страницы текстом.
- Длинные подписи створов («0,5 км ниже сброса ГКП "Атырау су арнасы"») — сокращение + тултип;
  тултипы Base UI над SVG проверить в первый день схемы. ← СНЯТО сессией 5:
  схема — DOM/flex, не SVG; усечение + тултип реализованы в `StationNode`.
- Deep-link: выбранное расследование и месяц в URL (защита: открыть экран закладкой, пережить F5).
- Печать проверять в блоке 36–45, не в последний час.
- Всё офлайн: никаких CDN (шрифт уже локален; тайлы карт — аргумент против MapLibre).
- React 19 / Vite 8 / TS 6 / ESLint 10 — примеры из интернета устарели в обе стороны,
  сверяться с установленными версиями.
