# Вопросы фронтенда к бэкенду

## Дописано 06.08.2026 — сессия 19: слияние `integration/mvp` и карта

### Предупреждение: в lockfile появился `maplibre-gl`

По прямому решению владельца продукта карта сделана на **MapLibre GL 6.2.0**,
пакет добавлен в зависимости `apps/web`. `package-lock.json` — твоя зона,
поэтому предупреждаю явно, чтобы правка не потерялась в следующем общем
коммите. Заодно в `apps/web/vite.config.ts` появились
`optimizeDeps.exclude: ['maplibre-gl']` и `worker.format: 'es'` — без них
воркер MapLibre не поднимается и слои карты не рисуются вовсе (симптом
обманчивый: маркеры видны, консоль чистая, а линий нет).

Цена: бандл вырос с 944 до 1884 kB (gzip 254 → 499).

### Ваш живой код расходится с вашей же фикстурой

`apps/api/src/live/live.mapper.ts` отдаёт **три** источника:
`kazhydromet-bulletins`, `gdelt`, **`direct-sources`**.
`packages/contracts/fixtures/live-status.json` содержит **четыре**:
`kazhydromet-bulletins`, `gdelt`, `open-meteo-flood`, `open-meteo-marine`.

Наш сид повторяет фикстуру, то есть в seed-режиме экран обещает два источника
Open-Meteo, которых в API нет, и не показывает `direct-sources`, который вы
реально опрашиваете. Какой список настоящий?

### Корневые команды сломались на Windows

После слияния `npm run typecheck` и `npm run build` из корня падают на
`apps/api`: скрипт `prisma:generate` использует bash-подстановку
`DIRECT_URL=${DIRECT_URL:-...}`, которую `cmd.exe` не разбирает. Я обхожу это,
гоняя всё с `-w web`, но команды документированы в `CLAUDE.md` как общие —
поправьте, пожалуйста, у себя.

### Координаты створов — теперь это блокер качества, а не только красоты

`Station.location` и `CandidateObject.location` везде `null`, поэтому карта
стоит на **демонстрационных** координатах, о чём на экране сказано в реестре
«Состояние данных». Если координаты с provenance появятся — удаляется ровно
одна таблица в `apps/web/src/constants/map.ts`, остальной код уже читает их
из модели. Ответ «не будет» устраивает, лишь бы он был заранее.

### Что я вычитал как ответы

- `GET /api/incidents` и `/:id` подняты — вопрос 21 закрыт технически.
  Данных при этом нет: `200 []`, human review `0/2`. Демо идёт на сиде.
- ruleset 1.2.0 → 1.2.1 и новый `inputHash` приняты; значения, уровни и
  утверждения в golden не изменились, сид править не пришлось.
- `reportedAt`/`publishedAt` в фикстурах переехали с `+05:00` на `Z` — тот же
  момент времени, наш вывод в `Asia/Atyrau` не задет.

Вопросы **15** (в `apps/web` нет ни `vitest`, ни `@playwright/test`) и **17**
(`packages/contracts` собирается в CommonJS) не сдвинулись — проверено по
`integration/mvp`.

---

Дата: 04.08.2026, после сессии 13. Автор: фронтенд (`apps/web`).
Отвечать можно прямо в этом файле или в чате.

## Дописано 05.08.2026 (вечер) — разбор ваших пушей: part-03/part-04 и ruleset 1.2.0

Сначала оргвопрос: **этот файл лежит только в ветке `frontend`**, и в ваши ветки
он не попадал — если вы его не видели, вопросы ниже (и весь список с 04.08) для
вас новые. Скажите, класть ли его в `main` общим коммитом, чтобы он был у всех.

### Что я взял

`feat/backend-investigation` влита в `frontend` без конфликтов: ruleset 1.2.0,
free-tier политика Gemini, corrective-миграция. Golden-результаты не поехали —
сентябрь L2 с двумя опровержениями, май L3, Актау L0 на месте, меняется только
версия ruleset в пункте 12 досье. Правило «максимум не выводится при
несопоставимых `unit`/`matrix`/`period`» принял как есть; на однородных данных
сентября оно ничего не меняет, но теперь сид обязан оставаться однородным.
Про `EvidenceStatement.code` как классификатор, а не идентификатор, — понял,
наши списки ключуются по `statement.id`, менять ничего не пришлось.

### Что я НЕ взял и прошу сделать вас

**`feat/backend-platform` я не сливал.** Слияние даёт 8 конфликтов в
`apps/api/prisma/schema.prisma` плюс миграции, seed, скрипты и `package.json`,
и это расхождение **двух бэкендов между собой**, а не с фронтом: Backend 1
добавил `StationRelationEvidence` и сделал `StationRelation.sourceDocumentId`
необязательным, Backend 2 — `metadata`, unique по
`(incidentId, inputHash, rulesetVersion)` и `InvestigationCandidateObjectEvidence`.

Сводить вашу схему БД я не буду: проверить результат мне нечем (нужен живой
PostgreSQL и ваши e2e), а тихо разъехавшаяся схема — худший из возможных
подарков. Слейте, пожалуйста, ветки между собой или в `main`, и я подтяну
результат одним merge. Фронту из платформенной ветки не нужно ничего:
`apps/web/**` она не трогала, `HealthReadySchema` у меня есть с сессии 14,
демо работает на сиде.

### Что я вычитал из ваших отчётов как ответы — поправьте, если понял неверно

1. **Порог `verified` у документов — подпись человека**, `requiredHumanReviewers: 2`.
   До `2/2` строгий seed не пишет вообще (`VERIFIED_DATA_HUMAN_REVIEW_INCOMPLETE`).
   Значит наш сид с настоящим sha256, но `status: 'unverified'` — честен,
   и я его не трогаю до появления подписей.
2. **`GET /api/incidents/:id` принимает `Investigation.id`** текущего снапшота,
   исторические id отдают 404. У нас id событий уже такой формы (`inv-atyrau-2025-09`).
3. **`investigation.corridor` всегда `null`** — геометрию не восстанавливаете.
   Наш коридор рисуется по `corridorBounds` (id створов), так что это совпало.
4. **`period` берётся из связанных измерений**, при разнобое — `null`.
5. **Неизвестный показатель приходит строкой «не определён»** — это уже готовый
   русский текст на экран, а не код; ок, но зафиксируйте, что он именно такой.
6. **Пустой список — честное состояние.** Принято: сейчас `GET /api/incidents`
   отдаёт `200 []`, потому что seed стоит на human-гейте, а current-снапшотов
   Backend 2 в общей базе нет.

### Новый вопрос

**Когда ждать human review `2/2`?** От этого зависит план демо: если подписи
не появятся, показываем на сиде (готов уже сейчас), и это нормально. Если
появятся — мне нужен день на F3/F5 и сверку чисел с сидом. Ответ «не успеем»
меня полностью устраивает, лишь бы он был заранее, а не в день показа.

### Что по-прежнему без ответа

Вопросы **15** (в `apps/web` нет ни `vitest`, ни `@playwright/test` — F8 не
с чего начинать) и **17** (`packages/contracts` собирается в CommonJS,
`main: dist/index.js` без `exports`/`module` — бандл фронта из-за этого
+350 kB) не сдвинулись: проверил по `feat/backend-platform` 05.08.2026.
Плюс висят 10 (формула цвета), 18 (`unknowns` в двух формах), 19 (`npm audit`),
20 (вывод Актау обеднел), 1а (место осетрового завода и Дамбы в цепочке),
8а (якоря `#page=N` на реальных URL Казгидромета).

## Дописано 05.08.2026 — ветка `feat/backend-investigation` влита в `frontend`

Спасибо, взял почти всё как есть: `code`/`sortOrder`, порядок четырёх створов
из проверенных связей, страницу 24 и sha256 обоих бюллетеней, дословные
выдержки, сентябрьский L2 с коридором и двумя опровержениями, майский L3,
Актау L0, сценарий реплея на шесть шагов. Сид теперь дословно повторяет
`data/fixtures/investigation/api/*` и golden ядра.

Что я **не** взял из ветки и почему (подробно — в `docs/decisions.md`, сессия 14):

1. **`use-selected-incident-detail.ts`: подмешивание `EvidenceGraph` в detail.**
   Граф несёт четыре измерения из семи и два документа из четырёх — то есть
   только то, на чём стоят утверждения. Подмена им detail убирает со схемы
   три створа (осетровый завод, Дамба) и выбрасывает из блока «Источники»
   публикации ТЗ §7.4, а весь экран становится заложником второго запроса.
   Соединю оба источника на этапе F3/F5.
2. **`src/api/exports.ts` + `apiDownload`.** Ходят в сеть мимо `VITE_DATA_MODE`,
   без таймаута, без `AbortSignal` и без нормализованной ошибки — мимо всей
   границы сети сессии 13. Наш `src/api/export.ts` уже умеет ваш эндпоинт
   в api-режиме и сид в офлайне.
3. **`DossierActions` в шапке правой панели.** Печатное досье живёт
   на `/dossier/:id` с сессии 12, «Скачать JSON» уже там; вторая пара кнопок
   ломает общую высоту трёх колонок. Серверный `format=html` не нужен —
   наш печатный маршрут сознательно не зависит от эндпоинта экспорта.
4. **Возврат `apps/web/src/types/*`.** Эти файлы удалены в сессии 13: формы
   данных приходят из `packages/contracts`, второй копии типов быть не должно.

Новые вопросы — 20, 21, 22 в `docs/frontend-plan.md`, коротко:

- **Вывод Актау обеднел.** В ядре пропала формулировка ТЗ §7.6 «есть сообщения
  и модель волн, но нет поля течений» — на экране «недостаточно данных» это
  была часть «что у нас есть». Вернуть в `conclusion` или так и оставить?
- **Ленты и события всё ещё нет.** `GET /api/incidents` и `GET /api/incidents/:id`
  не подняты, поэтому три ваших готовых эндпоинта включить не во что:
  `VITE_DATA_MODE=api` без ленты даёт пустой экран. Это блокер F3/F5.
- **Порог `verified` у документов.** sha256 настоящий, но статус всё ещё
  `unverified` и `fetchedAt: null`. Что должно появиться, чтобы документ стал
  `verified` — время скачивания или подписи двух человек?

Отдельно: расхождение фикстур `packages/contracts` (L1, statement
`MEASUREMENT_RECORDED`) и golden ядра (L2, два `contradicts`) осталось.
Сид я привёл к golden. Что из этого отдаст `GET /api/incidents/:id`?

> **Дописано вечером 04.08.2026, после появления ветки `feat/backend-investigation`.**
> Вопросы **1** (порядок створов — закрыт фикстурами `data/verified/*-station-relations.json`)
> и **7** (страница майского бюллетеня — 24, плюс sha256 и дословные выдержки)
> **закрыты**, спасибо. Ветку разберу следующей сессией.
>
> Взамен — одна просьба к Backend 2. В твоей ветке из `apps/web/src/api/investigations.ts`
> и `replays.ts` **удалены seed-ветки**, остались только запросы к API. Пожалуйста,
> не считай это решённым: критерий приёмки требует, чтобы демо работало полностью
> без бэкенда (`VITE_DATA_MODE=seed`), и обе ветки в каждом модуле — это осознанная
> конструкция сессии 13, а не забытая заглушка. При слиянии я оставлю оба режима
> и подключу твои эндпоинты второй веткой; твой `src/api/exports.ts` сведу
> с нашим `export.ts` в один модуль.
>
> Отдельно на сверку: фикстуры в `packages/contracts` остались на L1, а golden
> ядра даёт L2 — что из этого отдаст `GET /api/incidents/:id`?

**Сначала спасибо и отчёт.** Ветка `feat/backend-platform` влита в `frontend`,
фронт переехал на `@caspian-trace/contracts`: собственные типы удалены, ответы
и офлайн-сид проверяются вашими Zod-схемами, сентябрьский кейс приведён
к вашей фикстуре (L1, без коридора и без опровергнутой версии). Девять из
четырнадцати прежних вопросов закрылись самим пакетом — ниже только то,
что осталось, плюс новое, что вылезло при переезде.

Что закрылось: payload реплея (совпал дословно, включая офсеты), `period`
в списке, `corridorBounds`, формат ошибки, кейс Актау, состав досье,
`provenance`, владелец общего пакета, порт и CORS.

---

## Блокирует прямо сейчас

### 1. Порядок створов (`riverOrder`) — блокер №1

Нужен утверждённый порядок сверху вниз по течению для 7 сентябрьских
и 2 майских точек. Отдельно: где в этом порядке «посёлок Дамба» и «1 км выше /
1 км ниже Атырау» относительно точек «Атырау су арнасы».

В отчёте Part 01 это передано Backend 2 — прошу подтвердить, что вопрос
за ним, и назвать срок.

**Почему это важнее, чем кажется.** Раньше без `riverOrder` мы теряли только
вертикальную линию схемы. После вашей фикстуры от него зависит ещё и:
- сентябрьский **L2** (сейчас L1);
- **лента коридора** на схеме (сейчас участок не выделен);
- непустой блок **«Что не подтверждается»** — та самая исключённая версия,
  которую критерий приёмки называет главным содержанием демо.

Возражений против понижения нет: без подтверждённого порядка пространственный
вывод действительно недоказуем, и мы приняли вашу оценку целиком. Но сейчас
демо показывает «мы честно ничего не утверждаем» вместо «мы исключили версию
фактами», а это две разные истории. Ветки UI под коридор и исключённую версию
живы и проверены на майском кейсе — вернуть картинку можно правкой сида,
без единой строки кода.

### 2. Тестовая инфраструктура фронта

В `apps/web` нет ни `vitest`, ни `@playwright/test`, поэтому этап F8 (unit
на store реплея, projection, форматирование, `sourceHref`, уровни + E2E
на десятишаговый сценарий) не с чего начинать. У вас в `apps/api`
и `packages/contracts` jest уже стоит.

Ставите одним общим коммитом — или разрешаете добавить самому?

### 3. Предупреждение: я тронул `package-lock.json`

В `apps/web/package.json` добавлена workspace-зависимость
`@caspian-trace/contracts: 0.0.1` (ровно как в `apps/api`) плюс lifecycle-хуки
`predev`/`prebuild`/`pretypecheck`, собирающие contracts перед фронтом. Из-за
этого изменился и lockfile — по договорённости он ваш, поэтому предупреждаю
явно, чтобы правка не потерялась в вашем следующем общем коммите.

Новых внешних пакетов при этом не добавлено: zod приезжает транзитивно внутри
`@caspian-trace/contracts` и во фронте нигде не импортируется напрямую.

---

## Мешает, но обхожу

### 4. Фикстуры мая и Актау

Есть только сентябрьские. Майский detail и кейс Актау держу своими (майский L3
взят из вашего же `incidents.json`), но сверить их не с чем — и именно на майском
кейсе сейчас держится единственная в демо демонстрация коридора.

### 5. ESM-выход `packages/contracts`

Пакет собирается в CommonJS (`module: "commonjs"`). Два следствия для фронта:
- бандл вырос с ~523 kB до 884 kB (gzip 234 kB) — CJS почти не поддаётся
  tree-shaking, и в сборку попадают все схемы разом. Для офлайн-демо
  с локального диска терпимо, но это плата ни за что;
- dev-сервер Vite линкованный workspace-пакет не предбандливает и отдаёт как
  есть, поэтому именованные импорты падали с «does not provide an export named
  ApiErrorSchema». Обошёл через `optimizeDeps.include` в `vite.config.ts`.

Если добавите ESM-выход (`"module"`/`"exports"` + второй прогон tsc), оба
пункта уйдут. Не срочно.

### 6. `unknowns` существует в двух формах

`Investigation.unknowns` — массив строк, `Dossier.unknowns` — массив
`{code, text}` с кодом по маске `^[A-Z][A-Z0-9_]*$`. При сборке досье из сида
я подставляю `code: 'UNSPECIFIED'`, потому что таксономии кодов у нас нет
и выдумывать её нельзя.

Свести к одной форме — или скажите, кто присваивает коды и по какому списку
(в фикстуре встречается `STATION_ORDER_UNVERIFIED`).

### 7. Страницы майского бюллетеня

Подтвердить `source_pages` майских значений: сейчас `null`, и майские числа
открывают PDF целиком, без якоря. Плюс проверить на реальном URL Казгидромета,
отрабатывает ли `#page=N` или браузер принудительно скачивает файл — «каждое
число ведёт к источнику» это критерий приёмки, и у сентября якорь `#page=22`
сейчас единственная его демонстрация.

### 8. Формула «относительного положения» значения

Моё предложение: монохромная нормировка внутри одного события — длина бара
равна доле от максимума события, цветом ничего не кодируется. Красно-зелёной
шкалы «опасности» не будет в любом случае (запрет ТЗ). Нужно только согласие.

---

## К сведению

### 9. `react-router`: 2 high в `npm audit`

Вы оставили решение фронту. Решение: **перед демо не обновляю**. `audit fix
--force` ломает мажор роутера, на котором стоят все маршруты, deep-link
`?incident=` и печатный `/dossier/:id`; в офлайн-приложении без сервера риск
от обновления выше риска от самой уязвимости. Вернусь к этому после демо.

### 10. Что фронт готов принять хоть сегодня

Ветки `VITE_DATA_MODE=api` написаны и типизированы для всех шести эндпоинтов,
Vite proxy `/api → localhost:3000` настроен, ответы валидируются схемами,
ошибки нормализованы в `{code, message, requestId}`. Как только поднимутся
три read-эндпоинта — переключение режима это одна переменная окружения.

Проверю сразу же и сверю числа с фикстурами.
